# CODE-ADVERSARIAL — api-contract (search-ui, IMPLEMENTED)

Adversarial pass over code-panel/api-contract.md. Every finding tagged; ACC-1 independently re-verified LIVE (prod GET + lumen_read probe), ACC-2/3/4 verified at file:line.

| ID | Tag | Rationale |
|----|-----|-----------|
| ACC-1 | material | Live-reproduced on prod: `q=faith&scope=episodes&limit=23` mints cursor from JS-last `unshaken-ivzxaLpbZws#0` (search.ts:719-730 mints from `sortResults` last row, :705-713 sorts id by JS code-unit `a.id<b.id`), but SQL keyset `id > c.id` (:344-347) and leg `ORDER BY … id` (:363/367) use the column collation. DB probe: `datcollate=en_US.UTF-8`, `'Faith'<'faith'` → SQL false / JS true (inversion). Page-2 first row `unshaken-RLirbnj-kGk#2408` duplicates page-1 row 22 AND `(episode_id,t_start_s)` pair — raw API breach of F1/F15. Not blessed: harness pins `COLLATE "C"` only for scripture (non-diverging); episodes/topics ship uncollated. Fix (COLLATE "C" on ORDER BY id + keysetAfter) runs on computed subquery output → no index impact, not risky. |
| ACC-2 | noise | Documented + guarded, no shipped defect. (a) misnamed `cursor=` ignored — finding itself calls "acceptable" (web convention). (b) searchAll drops `after` when `scope.length!==1` (search.ts:744-747) is the documented contract (SearchOptions.after docstring :65-67) and the route already 400s `cursor_scope` first (api.search.tsx:82-84); `@lumen/scripture` is BRRU-5 verified-isolated so no other caller hits it. (c) loader omits `after` (search.tsx:248) is by-design first-paint SSR (pagination is fetcher-driven, :772). Proposed searchAll-throw is defensive hardening with no triggering caller. |
| ACC-3 | noise | Finding admits "unreachable today." Bind inputs are identical: route feeds parseQ's trimmed q (search-request.server.ts:24, Q_MAX rejects >200 at :25) into searchAll, which re-derives `(opts.q).trim().slice(0,200)` (search.ts:736) — a no-op on already-trimmed q≤200 → same cursorHash, both decodes agree. No current 500 path; pure future-drift belt-and-braces suggestion, no defect changing shipped quality. |
| ACC-4 | noise | Restates a ratified decision (CU-5 "bit-exact float64 score" incorporated, plan line 93) and proposes doc-note only, NO code change. Field stays `number` (search.ts:82); precision from score_bits (:679-682) is required for the cursor tiebreak and benign by the finding's own admission; no in-repo consumer reads `.score`. Flagging a blessed mechanism is not a finding. |

## Stance

Concur with the specialist on ACC-1 — its flagship finding. It is a genuine, live-reproduced correctness defect on the shipped public `/api/search` contract: within-page JS re-sort (code-unit) diverges from the SQL page boundary (en_US.UTF-8) inside score ties, so the minted cursor re-serves page-1 rows for the episodes and topics scopes. I reproduced the exact duplicate against prod and confirmed the collation inversion at the byte level; the UI's `dedupeMoments` masks it but raw API consumers see duplicates and gaps. Material, correctly rated high.

ACC-2/3/4 are all low-severity by the specialist's own labeling and, on verification, describe documented+guarded behavior (ACC-2), an admittedly-unreachable future-drift concern (ACC-3), and a ratified/benign mechanism with a doc-note-only ask (ACC-4). None is a shipped-quality defect; none proposes a fix that regresses; all are in-feature. Tagged noise. Net: 1 material, 3 noise — the panel surfaced one real bug and three hardening/documentation notes.

# Panel-2 — aggregated (unshaken-extraction A2)

Source of truth: per-role files. Verdicts: both NOT-approved-as-written; all material findings incorporated at synthesis (see plan.md ## Decisions).

eval-validity · A1: The eval packet cannot support the verdicts the gate needs — near-miss errors AND near-miss traps are unverifiable from ±2 utterances
eval-validity · A2: Trap-ness is detectable by artifact — the amended harness PINS the leak
eval-validity · A3: "Fresh-context agent + me" — the trap-seeder is on the catch jury, and the fresh agent isn't fresh
eval-validity · A4: Principle precision is unfalsifiable by a same-prior evaluator — the 0.80 gate must be conditioned on per-kind trap catch
eval-validity · A5: Gate arithmetic is undefined — allocation, operand, and trap accounting; at the plausible n, 0.90 is undecidable
eval-validity · A6: The iteration round re-grades against a leaked answer key
eval-validity · A7: Fixture vocabulary is one episode deep — corpus census refutes generalization; novel variants must surface at RUN time
eval-validity · A8: Foreign-window close semantics are undefined — an unclosed window silently swallows the rest of the episode
eval-validity · A9: The harness pins pre-first-segment fallback to "stamp the first chapter" — a guess graded as truth
eval-validity · A10: Agent-produced alias tables need deterministic validation — census membership, pool membership, collision handling
eval-validity · A11: A1 repair migration — validate the unwrap, close the double-wrap hole, scope entities, and make repaired-state a load prereq
eval-validity · A12: "Journaled resume = per-agent retry native" is asserted, not probed — the workflow substrate doesn't exist yet
eval-validity · A13: Plan text still instructs a Batch-API build — direct contradiction of the Revision-1 directive
eval-validity · A14: Trap/gold items and the 120-sample interplay with kind strata is unstated in the harness
eval-validity · A15: Evaluator wall-clock and cost under sharding
prod-write-safety · A1: The A1-rerun co-fix cancels itself — source-aware delete removes the very rows whose mentions the upsert is supposed to preserve
prod-write-safety · A2: A2's second run silently destroys its own extraction edges — the classification fetch is unpinned and panel-1's literal recommendation causes the loss
prod-write-safety · A3: Repair ordering is unenforced, and two of three plausible title-UPDATE implementations corrupt or abort on unrepaired rows
prod-write-safety · A4: Title-edge mentions replace-vs-append is unpinned — an append implementation passes the harness and breaks idempotency + alias-staleness
prod-write-safety · A5: Per-episode tx is mandated by one plan sentence, enforced by nothing — and the promised wipe-canary smoke invariant fell out of the plan
prod-write-safety · A6: Nothing binds the eval verdict to the artifacts that load — and no artifact carries an upstream freshness fingerprint
prod-write-safety · A7: "Confidence floor + lens threshold" is not a blast-radius argument for in-vocab errors — and the one cheap deterministic gate is missing
prod-write-safety · A8: Concurrency residuals — no corruption path found
prod-write-safety · A9: Smoke-plan coverage verdict (task-mandated verification)

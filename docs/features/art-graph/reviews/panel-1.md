# Panel-1 aggregate — art-graph

## api-contract
# Panel-1 / api-contract review — art-graph

| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| API-1 | high | plan.md L48-50 (Card stack) vs `ArtItem`/`toArtItem` (scripture.tsx L66-101) | Stack needs fame to rank cards (`pickArtStack`), but `toArtItem` — the only converter feeding the chapter loader's `art` field — drops `metadata.fame` entirely. | Add `fame: number \| null` to `ArtItem` and `toArtItem`, or have the stack consume raw `ArtworkRow`s directly. |
| API-2 | high | plan.md L89-90 (FM-3: "exhaustive test against live-probed id list snapshot") vs `art-edges.test.mjs` L45-54 | Harness only checks 2 of ~30 mapped slugs against a 3-entry inline mock array, not an exhaustive test against the live person-id snapshot the plan promises. | Add a real exhaustive test: assert every `ART_PERSON_MAP` value is in the probed live person-id list from L31. |
| API-3 | med | plan.md L40-44 (DEPICTS) vs `packages/scripture/src/slug-map.ts` L169-177 (`RELATIONSHIP_TYPES`) | `RELATIONSHIP_TYPES` is the codebase's exported, test-enforced rel_type allowlist (`FEATURES` is already in it); plan never adds `DEPICTS`, so the canonical list silently omits the new type. | Add `'DEPICTS'` to `RELATIONSHIP_TYPES` in slug-map.ts and its exhaustive-membership test. |
| API-4 | med | plan.md L69-70 (new route) vs `scripture.tsx` loader L321-348 | Plan doesn't say whether `scripture.art.tsx`'s loader replicates the chapter loader's canonical-URL 301 (alias book → canonical bookId); harness only tests unknown-book 404, not the redirect. | State explicitly whether the gallery route redirects aliases (e.g. `1ne`) to the canonical book slug, and test it. |
| API-5 | low | plan.md L53 (gallery cards: "image, title, artist · year, source link") | Card field list omits `thumb`/`image_url` fallback behavior (`ArtImage`'s onError swap) that the strip relies on for broken 800px thumbs; unspecified whether gallery reuses `ArtImage` or reimplements. | State that gallery cards reuse the existing `ArtImage` component (thumb-with-fallback), not a new image element. |
| API-6 | low | plan.md L78-79 (Public contract: "chapter page keeps `art` loader field") | True for the strip's fields, but doesn't reconcile with API-1 — claims "no loader change" while the stack (same section) needs `fame`, which isn't in that field's current shape. | Cross-reference the fame requirement here so the "no loader change" claim is accurate once API-1 is fixed. |

## correctness
| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| COR-1 | High | Failure mode 2 / FM-2 test (art-edges.test.mjs:29-43) | Only tests verse_start=8,end=14; no test for verse_start set, verse_end null (single-verse cite) — likely the common case among the 766 verse-tagged works. | Add a test: verse_start=16, verse_end=null → 1 verse edge at that verse, defaulting end to start (mirror scripture.tsx:577 `r.verse_end ?? r.verse_start`). |
| COR-2 | High | Failure mode 1/3, Live facts | verseExists is passed to buildArtEdges but no test exercises a verse that fails it; plan never states expected/measured fraction of the 766 verse refs invalid against KJV spine (catalog versification drift). | Add a rejected-verse unit test + log rejection count/fraction in ingest output; state expected ceiling in plan. |
| COR-3 | Med | Prior learnings row 1; Live facts (jesus-1 vs jesus-christ) | Plan names the jesus-1/jesus-christ ambiguity but never states the decision rule or verification for why 'jesus-christ' (not 'jesus-1') is the correct FEATURES target. | Add one line to plan/ART_PERSON_MAP documenting how the correct id was verified (e.g. connectivity/usage in existing edges). |
| COR-4 | Med | Failure mode 1 (only book-invalid tested) | FM-1's only invalid-ref test uses an unmapped book ('tobit'); no test for chapter 0 or a chapter number exceeding a valid book's real chapter count. | Add a chapterExists-false unit test with a valid book + out-of-range chapter; assert it's skipped+counted, not guessed. |
| COR-5 | Low | Failure mode 4 / dedupe test (art-edges.test.mjs:65-75) | is_primary merge is tested only true-then-false ("primary wins"); order-independence and range_start/range_end merge on duplicate overlapping verse refs are unspecified. | Add a false-then-true ordering test and define/test range metadata merge policy for duplicate overlapping verse refs. |
| COR-6 | Low | Files touched (scripture.art.tsx "+ routes config") | No explicit routes.ts detail or test confirming the new 4-segment `/scripture/:book/:chapter/art` coexists with the existing 3-segment chapter route. | State route registration explicitly in plan; add a route-matching smoke assertion (or note React Router ranks static > dynamic, so no collision). |

## data-integrity
# Panel 1 — data-integrity review: art-graph

| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| DATA-1 | High | Scope §1 + Failure modes 1–2 | No failure-mode/test for verse_start/verse_end exceeding the chapter's real max, and no tolerance cap (unlike openbible's 0.5% FM-11) — one bad ref hard-aborts the whole ~15k-edge tx via the in-tx invariant. | Add unit test: verseExists must gate per-target-verse (not just chapterExists); add a skip-ratio cap so isolated bad refs are skipped+counted, not fatal. |
| DATA-2 | Medium | Public contract / Out-of-scope "Neo4j mirroring" | `backfill-neo4j-collections.mjs` queries `lumen.entities`/`lumen.edges` unfiltered by collection, so ~4,461 artwork entities + ~15k DEPICTS/FEATURES edges become permanently unmatched "missing" against Neo4j — undocumented as a plan side effect. | Document expected new artwork/DEPICTS/FEATURES entries in backfill's known-missing baseline (comment + run book) so real regressions stay distinguishable. |
| DATA-3 | Medium | Scope §4 smoke spot checks | Smoke only spot-checks one chapter-level DEPICTS and one FEATURES edge; the verse-range expansion path (766 works, the most complex logic) has no live spot check. | Add a live spot check asserting a known verse-range work produced correct per-verse DEPICTS rows + range_start/range_end metadata. |
| DATA-4 | Low | Public contract (delete scope) | Delete scope "collection_id='art' AND rel_type IN (...)" is stated without naming `lumen.edges`; collection 'art' also labels artwork rows in `lumen.entities`, inviting a future misapplied delete. | State the DELETE explicitly against `lumen.edges` in plan/script header, matching the openbible script's explicit table-qualified phrasing. |

## observability
| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| OBS-1 | Medium | Scope §1 (materialize-art-edges.mjs, "unmatched slugs counted + reported") | No event name/shape specified for unmapped FEATURES slugs or skipped DEPICTS refs, unlike `openbible_unmapped_refs` {count, ratio, sample:10} precedent. | Name events (e.g. `art_edges_unmapped_slugs`, `art_edges_skipped_refs`) with count + first-10 sample, mirroring ingest-openbible-refs.mjs's shape. |
| OBS-2 | Medium | Scope §1 ("delete-collection-CROSS-rel-types-then-insert idempotency") + Scope §4 ("re-run stability marker") | Smoke requires a re-run stability marker but no `lumen.migration_state` key is named (openbible used key `'openbible-ingest'`); nothing to compare against. | Name the key explicitly (e.g. `'art-edges-materialize'`) storing {at, inserted, deleted, counts-by-rel_type} for smoke to diff. |
| OBS-3 | High | Scope §4 ("a Luke 2 nativity work has DEPICTS luke-2; jesus-tagged work FEATURES jesus-christ") | No concrete artwork id/title named, unlike openbible's live-probed `gen-1-1 → heb-11-3 votes=271`; if no nativity work maps to luke-2 the check is either brittle or silently tautological. | Probe now and name a specific artwork id (like Gen 1:1→Heb 11:3) in the plan before the smoke script is written. |
| OBS-4 | High | Scope §3 (gallery loader "reuses `getChapterArt`") + Failure mode #6 | Plan never states behavior when `getChapterArt` throws for the gallery route; chapter page silently swallows to `[]` with no log, and #6 conflates that with genuine "no art" empty-state. | Gallery loader must log a named degraded event (e.g. `art_gallery_degraded`) on catch, distinct from true empty-state. |
| OBS-5 | Low | Scope §3 ("404 on invalid chapter") | No named log event for the gallery route's 404, unlike `scripture_404`'s cause taxonomy (`unknown_book`/`invalid_chapter`/`empty_chapter`) on the chapter route. | Log a named event (e.g. `art_gallery_404` {cause, book, chapter}) before throwing 404, matching the `scripture_404` convention. |
| OBS-6 | Low | Files touched (scripture.tsx stack + scripture.art.tsx gallery) | Plan doesn't state the new card stack / gallery cards reuse `ArtImage`'s thumb→full onError fallback; `ChapterArtStrip` (being replaced) is the only caller named. | Explicitly state stack and gallery cards call the existing `ArtImage` component (export/share it) to preserve the fallback. |

## security
| ID | Severity (low/med/high) | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| SEC-1 | high | Scope §2 Gallery route — "grid of cards (image, title, artist · year, outbound source link)" | `metadata.source_url` (raw catalog string, no scheme check anywhere) renders straight into `<a href>` (`scripture.tsx` strip pattern); a `javascript:` value executes on click, and the new route triples exposure (100 rows, indexable URL) vs the strip's 12. | Validate `source_url`/`image_url` are `http(s):` before use (ingest-time reject or render-time allowlist helper shared by strip + gallery). |
| SEC-2 | low | Scope §1 Edge materialization / existing `ArtImage` reused by gallery | `image_url`/`thumbnail_800_url` feed `<img src>` unvalidated; a `data:`/`javascript:`-prefixed value can't execute via `src` but can break the fallback swap or serve as a phishing/tracking vector at scale. | Same scheme allowlist as SEC-1, applied at ingest (`ingest-art-catalog.mjs`) so both `img` and `a` consumers inherit clean data. |
| SEC-3 | med | Failure modes #1–2 — "invalid book → skipped+counted" has no analog for verse-range bounds | Ref `verse_start`/`verse_end` come from the downloaded catalog untrimmed; a bogus range (e.g. `verse_end: 500` on a 20-verse chapter) creates orphaned verse edges caught only by the in-tx `zero orphan endpoints` invariant, which **aborts the whole tx** — one bad catalog row can DoS the entire re-run. | Bound verse_start/verse_end against live per-chapter verse counts before expansion (mirror `ingest-openbible-refs.mjs`'s `verseCount`-gated `expandOsisRange`); skip+count out-of-range refs like FM-1's book handling instead of hard-aborting. |
| SEC-4 | low | Public contract — "collection_id='art' (collection row exists)" | Plan never states the materialize script sets `collections.public = true` on the art row; `ingest-art-catalog.mjs`'s original insert also omits it, diverging from the openbible ingest's explicit CSEC-5 precedent ("public visibility explicit, not schema-default"). | Have the materialize script's collection upsert explicitly set `public = true`, matching `ingest-openbible-refs.mjs`'s `ON CONFLICT DO UPDATE ... public = EXCLUDED.public`. |

## ux-a11y
| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| UX-1 | High | Scope §2 "Card stack" (compact overlapping stack, "+N" count chip) | No accessible name/role defined; overlapping cards could each be separately focusable, producing overlapping/hidden tab stops instead of one target. | Spec stack as a single `<button aria-label="View {N} artworks for {reference}">`; individual overlapping images `aria-hidden`, not separately focusable. |
| UX-2 | High | Scope §2; Failure mode #7 (stack renders/top card/+N — pure helper tests) | Overlap by design shrinks each card's clickable footprint below 44px; helper tests cover fame/count logic but not tap-target geometry. | Define one 44px-min tap target for the whole stack (per UX-1's single button), not per-card zones; add to harness as a rendered-size assertion. |
| UX-3 | Medium | Scope §2 (no animation spec given) | "Stack" visually invites a hover/tap fan-out reveal, but plan specifies no animation and no `prefers-reduced-motion` gate if one gets added during build. | Explicitly spec stack as static (no fan/expand transform), or gate any hover animation behind `prefers-reduced-motion` in the harness checklist. |
| UX-4 | High | Scope §3 "Gallery route" (responsive grid of cards, unknown aspect ratios) | Plan doesn't specify grid strategy; masonry with unknown image aspect ratios causes CLS on image load and can visually reorder cards away from DOM/tab order. | Use CSS grid with fixed `aspect-ratio` placeholder boxes (not JS/CSS masonry); tab order must match visual order. |
| UX-5 | Medium | Scope §3 "breadcrumb back to the chapter" | Plan defines the forward path (stack → gallery) but not the return path; navigating away and back likely drops scroll position and selected verse. | Preserve chapter scroll/selected-verse state on back nav (browser back or explicit state pass), not just a breadcrumb link to the bare chapter URL. |
| UX-6 | Medium | Files touched: `scripture.tsx` (strip → stack); header already has Back, title, chapter nav, Graph button | Chapter header is already dense on mobile; plan doesn't address where the stack sits relative to existing header rows or its height budget. | Place stack as a fixed-height row directly under nav links; cap height so header never pushes chapter summary off first mobile viewport. |
| UX-7 | Medium | Goal + Scope §2 (strip replaced by 3-card stack + click-through) | Honest regression: today's strip shows up to 12 works always-visible and scrollable; a 3-card stack behind a click hides most art for casual/passive discovery. | Accept trade-off explicitly in plan, or show 4-5 cards on desktop / keep top card larger so more art previews without a click. |


| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| UX-1 | High | Scope §2 "Card stack" (compact overlapping stack, "+N" count chip) | No accessible name/role defined; overlapping cards could each be separately focusable, producing overlapping/hidden tab stops instead of one target. | Spec stack as a single `<button aria-label="View {N} artworks for {reference}">`; individual overlapping images `aria-hidden`, not separately focusable. |
| UX-2 | High | Scope §2; Failure mode #7 (stack renders/top card/+N — pure helper tests) | Overlap by design shrinks each card's clickable footprint below 44px; helper tests cover fame/count logic but not tap-target geometry. | Define one 44px-min tap target for the whole stack (per UX-1's single button), not per-card zones; add to harness as a rendered-size assertion. |
| UX-3 | Medium | Scope §2 (no animation spec given) | "Stack" visually invites a hover/tap fan-out reveal, but plan specifies no animation and no `prefers-reduced-motion` gate if one gets added during build. | Explicitly spec stack as static (no fan/expand transform), or gate any hover animation behind `prefers-reduced-motion` in the harness checklist. |
| UX-4 | High | Scope §3 "Gallery route" (responsive grid of cards, unknown aspect ratios) | Plan doesn't specify grid strategy; masonry with unknown image aspect ratios causes CLS on image load and can visually reorder cards away from DOM/tab order. | Use CSS grid with fixed `aspect-ratio` placeholder boxes (not JS/CSS masonry); tab order must match visual order. |
| UX-5 | Medium | Scope §3 "breadcrumb back to the chapter" | Plan defines the forward path (stack → gallery) but not the return path; navigating away and back likely drops scroll position and selected verse. | Preserve chapter scroll/selected-verse state on back nav (browser back or explicit state pass), not just a breadcrumb link to the bare chapter URL. |
| UX-6 | Medium | Files touched: `scripture.tsx` (strip → stack); header already has Back, title, chapter nav, Graph button | Chapter header is already dense on mobile; plan doesn't address where the stack sits relative to existing header rows or its height budget. | Place stack as a fixed-height row directly under nav links; cap height so header never pushes chapter summary off first mobile viewport. |
| UX-7 | Medium | Goal + Scope §2 (strip replaced by 3-card stack + click-through) | Honest regression: today's strip shows up to 12 works always-visible and scrollable; a 3-card stack behind a click hides most art for casual/passive discovery. | Accept trade-off explicitly in plan, or show 4-5 cards on desktop / keep top card larger so more art previews without a click. |

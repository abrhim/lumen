| ID | Severity (low/med/high) | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| SEC-1 | high | Scope §2 Gallery route — "grid of cards (image, title, artist · year, outbound source link)" | `metadata.source_url` (raw catalog string, no scheme check anywhere) renders straight into `<a href>` (`scripture.tsx` strip pattern); a `javascript:` value executes on click, and the new route triples exposure (100 rows, indexable URL) vs the strip's 12. | Validate `source_url`/`image_url` are `http(s):` before use (ingest-time reject or render-time allowlist helper shared by strip + gallery). |
| SEC-2 | low | Scope §1 Edge materialization / existing `ArtImage` reused by gallery | `image_url`/`thumbnail_800_url` feed `<img src>` unvalidated; a `data:`/`javascript:`-prefixed value can't execute via `src` but can break the fallback swap or serve as a phishing/tracking vector at scale. | Same scheme allowlist as SEC-1, applied at ingest (`ingest-art-catalog.mjs`) so both `img` and `a` consumers inherit clean data. |
| SEC-3 | med | Failure modes #1–2 — "invalid book → skipped+counted" has no analog for verse-range bounds | Ref `verse_start`/`verse_end` come from the downloaded catalog untrimmed; a bogus range (e.g. `verse_end: 500` on a 20-verse chapter) creates orphaned verse edges caught only by the in-tx `zero orphan endpoints` invariant, which **aborts the whole tx** — one bad catalog row can DoS the entire re-run. | Bound verse_start/verse_end against live per-chapter verse counts before expansion (mirror `ingest-openbible-refs.mjs`'s `verseCount`-gated `expandOsisRange`); skip+count out-of-range refs like FM-1's book handling instead of hard-aborting. |
| SEC-4 | low | Public contract — "collection_id='art' (collection row exists)" | Plan never states the materialize script sets `collections.public = true` on the art row; `ingest-art-catalog.mjs`'s original insert also omits it, diverging from the openbible ingest's explicit CSEC-5 precedent ("public visibility explicit, not schema-default"). | Have the materialize script's collection upsert explicitly set `public = true`, matching `ingest-openbible-refs.mjs`'s `ON CONFLICT DO UPDATE ... public = EXCLUDED.public`. |

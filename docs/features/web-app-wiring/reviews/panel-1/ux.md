| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| UX-1 | high | `?verse=N` cross-refs panel | Full SSR reload per verse click with no scroll anchor spec — clicking verse 40 of Psalm 119 likely resets scroll to top, losing reading position. | Use `#verse-N` fragment + `ScrollRestoration`/anchor scroll, or state that scroll position to clicked verse is preserved on reload. |
| UX-2 | high | Cross-refs panel contract | No stated behavior for panel dismissal/return-to-reading — plan never says how a user closes the panel or gets back to plain chapter view. | Specify a close affordance (link back to `/scripture/:book/:chapter`) and where focus lands after closing. |
| UX-3 | med | Chapter route `GET /scripture/:book/:chapter` | No prev/next chapter navigation in the contract — reading Psalm 119 to completion requires manual URL editing between chapters. | Add prev/next chapter links in the contract; cheap win, core to "reading experience basics." |
| UX-4 | med | Neo4j degraded state | "graph features unavailable" message wording/placement unspecified — could read as a generic error and undermine confidence in the whole page rather than just cross-refs. | Specify inline, scoped copy near cross-ref entry points (e.g., verse links or panel), not a page-level banner. |
| UX-5 | med | Verse list rendering | Plan doesn't specify verse links get visible focus styles or that they're reachable via keyboard in document order — accessibility fundamental for this panel's scope. | State verse links are real anchors with visible `:focus-visible` styles, no JS-only handlers. |
| UX-6 | med | Reference header / landmarks | No mention of heading hierarchy or landmark structure (`<main>`, `<h1>` for reference, `<nav>` for cross-refs) — root.tsx's error boundary already skips this pattern. | Specify one `<h1>` per chapter reference, `<main>` landmark, cross-refs in a labeled `<nav>` or `<section>`. |
| UX-7 | med | 176-verse chapters (e.g. Psalm 119) | Long verse lists with no reading-width constraint mentioned — "readable Tailwind" is vague and risks full-bleed text at wide viewports, hurting scanability. | Add explicit max-width (e.g. `max-w-prose`) requirement to the styling line of the contract. |
| UX-8 | low | Missing summary case | "render chapter without summary block" gives no copy guidance — silent omission may read as a bug rather than intentional absence to a reviewer/tester. | Non-blocking for plain proof-of-stack; optional placeholder copy only if trivial. |
| UX-9 | low | Cross-ref source attribution | Plan lists "curated vs ai-generated" as a data field but doesn't specify how it's surfaced (label, icon, tooltip) — risks unlabeled/inaccessible markers. | Require a plain-text label per cross-ref (e.g. "AI-generated"), not color/icon-only. |
| UX-10 | low | Unknown book/chapter 404 | Plan says "404 (RR7 error boundary)" but root.tsx's boundary is generic ("could not be found") — no path back to a valid chapter or the volume list. | Add a link to `/` or the book's chapter 1 in the 404 state. |

| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|----|----------|-------|------------------------|-------------------|
| COR-1 | high | Plan: "KV-backed cache for Neo4j reads" | wrangler.json has no `kv_namespaces` binding, so `context.cache` is undefined at runtime; only tests mock it, code path unverified against real config. | Add `kv_namespaces` block to wrangler.json and assert `env.<BINDING>` exists before building `AppLoadContext.cache`. |
| COR-2 | high | Plan: "Module-scoped lazy postgres singleton" / `db.server.test.ts` | Test proves single-caller reuse and sequential retry, but not concurrent cold-start: two simultaneous requests can both see unset singleton and both call `postgres()`, leaking a connection. | Guard singleton assignment with a synchronous "in-progress" flag or assign the client object before first await so second caller reuses it. |
| COR-3 | med | Plan: verse id `${bookId}-${chapter}-${verse}` vs `slug-map.ts` `buildVerseId` | Plan says loader constructs verse id inline instead of reusing exported `buildVerseId()`, and `queries.ts getChapterSummary` builds `${bookId}-${chapter}-summary` independently — three separate ID-format sites. | Route and `getChapterSummary` must call `buildVerseId`/shared ID helpers instead of re-deriving the format inline. |
| COR-4 | med | Plan: "KV-backed cache ... 7-day TTL, key xrefs:{verseId}" | Cache key has no version/schema component; if `findCrossReferences` query or relationship data changes, stale entries silently serve outdated cross-refs for up to 7 days with no invalidation path. | Prefix cache key with a schema/version tag (e.g. `xrefs:v2:{verseId}`) bumped on query/shape changes. |
| COR-5 | med | Plan: "Security headers applied to all responses in the worker fetch handler" vs `workers/app.ts` | Current `workers/app.ts` returns `requestHandler(...)` result directly (line 19); loaders that `throw new Response(..., {status:404})` bypass any wrapping added only around the success path, so 404s may miss security headers. | Wrap the single `requestHandler` return value (RR7 already funnels thrown Responses through it) so headers apply uniformly to all statuses. |
| COR-6 | med | Failure mode 7 vs harness (`scripture.loader.test.ts` cache-hit test) | Cache-hit test only exercises one sequential request; no coverage for concurrent first-time requests for the same verse, which would cause a thundering-herd of duplicate Neo4j calls before cache is populated. | Note as accepted gap or add single-flight de-dup keyed by verseId per isolate; at minimum document as known limitation. |
| COR-7 | med | `neo4j-http/src/client.ts` execute() vs plan's "Neo4j failure → graphDegraded: true" | `Neo4jAuthError`/`Neo4jQueryError` and JSON-parse failures all throw synchronously, but `AbortSignal.timeout(20_000)` means a hung Aura instance (72h-idle wake-up per plan's own constraint) blocks 20s before degrading — plan doesn't state whether this fits the loader's degradation budget. | Document/confirm 20s timeout is acceptable for user-facing latency, or lower it and let cache/graceful-degradation absorb slow wake-ups. |
| COR-8 | low | `slug-map.ts parseReference` chapter-level vs route's own `/^\d+$/` check | Plan validates chapter via route regex separately from `parseReference`'s own numeric parsing (lines 124–128, 130–132), so two independent number-parsing paths can diverge (e.g. leading zeros, `+1`) since regex is stricter than `parseInt`. | Route should rely solely on `parseReference`'s book/chapter extraction instead of a second ad hoc regex, or document why both exist. |
| COR-9 | low | Plan failure mode 8: "init-failure retry on next call" | Test only covers `postgres()` constructor throwing synchronously; doesn't cover a client that constructs but whose connection is dead (Hyperdrive proxy restart) — singleton would keep returning a broken cached client indefinitely. | Add a lightweight liveness check or catch query-level connection errors and clear the singleton to force reconstruction. |
| COR-10 | low | `find-cross-references.ts` uses `neo4j.layer.lumen.query` | `layerProxy` throws synchronously (`Unknown layer`) if `layers.lumen` isn't configured in the per-request client factory; plan doesn't call out this as a distinct failure mode separate from network/auth errors, though harness's generic "Neo4j throws" test would still catch it — worth naming explicitly. | Add explicit test/assertion that missing `layers.lumen` config degrades gracefully like other Neo4j failures, not just network errors. |

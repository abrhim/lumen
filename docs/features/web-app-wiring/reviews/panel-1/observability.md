| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| OBS-1 | high | Graceful degradation catch (`graphDegraded: true`, plan L18/42, scripture.loader.test.ts "degrades gracefully") | Neo4j failure is caught and swallowed into a boolean; no log statement anywhere in plan/tests captures the error, `Neo4jQueryError.code`, or `Neo4jAuthError` cause before it's discarded. | In the catch, `console.error(JSON.stringify({ event: "neo4j_degraded", code, message, bookId, chapter }))` before setting `graphDegraded: true`. |
| OBS-2 | high | KV cache failure fallthrough (plan L16, "cache failures fall through to live query", test "KV get failure still serves…") | KV `get`/`put` errors are fully silent — no signal distinguishes "cache miss" from "cache broken," so a dead KV namespace degrades to live-query-every-request forever, invisibly. | Log a single structured line on KV catch (`{event:"kv_cache_error", op, key}`) even though the request proceeds normally. |
| OBS-3 | high | Neo4j down + PG down asymmetry (plan L43 vs L44) | Neo4j silent 200 degradation has zero required log point in the plan; only PG failure (500 boundary) is naturally visible via the error boundary. Neo4j is invisible by design as flagged in the lens. | Require one `console.log`/`console.error` JSON line in the Neo4j catch path as an explicit deliverable, not just the `graphDegraded` field. |
| OBS-4 | med | Cache hit/miss (plan L16, KV-backed cache, 7-day TTL) | No hit/miss signal is emitted anywhere (loader, cache.server.ts spec, or tests) — Phase 5 has nothing to build a cache hit-rate metric from without re-touching this code later. | Emit one field per response, e.g. `context.cache` records `source: "cache"` \| `"live"` \| `"error"`; log or return it for later aggregation. |
| OBS-5 | med | DB/query latency (db.server.ts, neo4j.server.ts — not in plan at all) | Plan has no mention of timing Postgres or Neo4j calls. Without a duration field now, Phase 5 dashboards can't backfill latency history once observability lands. | Wrap `db.execute`/`neo4j` calls with `Date.now()` deltas and include `duration_ms` in the same structured log line as OBS-1/OBS-3. |
| OBS-6 | med | Error logging shape — undefined across the whole plan | `observability.enabled: true` in wrangler.json only captures Workers Logs from stdout/stderr — but the plan never specifies a structured log shape (JSON with request id, route, error code), so any logging added ad hoc during implementation risks unstructured strings that Workers Logs can't filter on. | Define one shared `logError(event, fields)` helper in workers/app.ts or a small `log.server.ts` used by every catch site, emitting single-line JSON. |
| OBS-7 | med | Init-error pattern for singleton (plan L65, "catch→store→re-throw for singleton construction failures") | Postgres singleton construction failure is only specified to be re-throwable for retry — no requirement to log the first-failure cause, so a wedged singleton fails 500s with no diagnosable reason in Workers Logs. | Log the causal error once at construction-catch time (`console.error({event:"pg_singleton_init_failed", message})`) alongside the re-throw. |
| OBS-8 | med | 404 vs degraded ambiguity for a bug report (public contract L39/42) | A user reporting "cross-references missing" produces identical HTML/behavior whether caused by Neo4j down, KV down-but-live-query-empty, or a legitimately absent cross-ref — no per-request marker distinguishes these from a bug report alone. | Include `graphDegraded`/cache-source as an HTML comment or response header in dev/staging so a screenshot + view-source is diagnosable. |
| OBS-9 | low | Security headers middleware (plan L18/44) | No mention of logging when headers middleware itself errors or is bypassed (e.g., early return before headers applied) — a header regression would only be caught by an external scanner, not logs. | Not urgent for Phase 1; note as a Phase-5 synthetic-check candidate rather than a code hook now. |
| OBS-10 | low | `Neo4jQueryError`/`Neo4jAuthError` (packages/neo4j-http/src/errors.ts) not referenced by plan | Errors carry a useful `code`/`name` distinction (auth vs query) but the plan's degradation catch (L18) doesn't say whether it inspects `error.name`/`code` or discards the whole object into a boolean. | In the catch, branch/log on `error.name` (`Neo4jAuthError` vs `Neo4jQueryError` vs generic) so auth misconfig isn't confused with transient timeouts later. |

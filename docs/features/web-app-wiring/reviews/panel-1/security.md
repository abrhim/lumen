| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|----|----------|-------|------------------------|-------------------|
| SEC-1 | high | Hyperdrive/`db.server.ts` (plan: "postgres superuser role, bypasses RLS") | Plan connects via Hyperdrive as PG superuser, bypassing RLS entirely; any future write/query path skips the public-read-only guardrail the DB layer provides. | Use a scoped read-only DB role for Hyperdrive instead of superuser; keep superuser only for migrations, not the app runtime. |
| SEC-2 | med | `apps/web/.dev.vars` (working tree, not in plan's file list) | Live-looking `NEO4J_PASSWORD` sits in plaintext in the working tree; gitignored but the plan never mentions rotation/handling if it's ever committed or shared. | Confirm value is non-prod or rotate before any real deploy; add a pre-commit secret scan given repo is public. |
| SEC-3 | med | Security headers list (plan: X-Content-Type-Options, X-Frame-Options, HSTS, Referrer-Policy) | No Content-Security-Policy is planned despite rendering AI-generated chapter summaries and Neo4j-sourced cross-reference text into HTML. | Add a CSP header (even a conservative default-src 'self') to reduce stored/reflected XSS blast radius from DB-sourced content. |
| SEC-4 | med | `GET /scripture/:book/:chapter` — verse/summary/cross-ref text rendering | Plan doesn't state that verse text, chapter summaries, or cross-reference text are escaped/sanitized before render; AI-generated summaries are a plausible injection vector if stored unsanitized. | Confirm React's default JSX escaping is used (no `dangerouslySetInnerHTML`); explicitly note this in the route implementation. |
| SEC-5 | med | KV cache key derivation ("key derived from verse id") | Plan doesn't specify the exact key construction; if raw `?verse=` or book/chapter path segments are concatenated without the parsed/validated numeric values, it risks cache-key injection or cross-entry poisoning. | Derive cache key only from validated `bookId`/`chapter`/`verse` integers (post-`parseReference`), never raw request strings. |
| SEC-6 | low | `explore-graph.ts` depth interpolation (cited as "not exposed in this feature") | Plan relies on `exploreGraph`'s unparameterized `[*1..${depth}]` Cypher depth interpolation staying unreachable; no guard exists to keep future routes from wiring user input into it. | Add a code comment / lint rule marking `exploreGraph` depth as trusted-input-only until it gets real bounds validation. |
| SEC-7 | low | `wrangler.json` vars (NEO4J_URI, NEO4J_USER, SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY) | Plan confirms these are intentionally public, but repo being public plus a real Neo4j Aura instance ID/hostname in `vars` gives an attacker a concrete target to probe/brute-force. | Acceptable per design; add rate-limiting or Aura IP allowlisting out-of-band since the endpoint is now publicly discoverable. |
| SEC-8 | low | Graceful degradation (`graphDegraded: true` path) | Plan doesn't specify whether Neo4j error details (message/stack) ever reach the response body/loader data; leaking internal error text aids reconnaissance. | Ensure caught Neo4j errors are logged server-side only; loader returns just the `graphDegraded` boolean, no error message/stack. |
| SEC-9 | low | KV cache (`CACHE` binding, plan: "edit — KV binding CACHE") | No mention of KV namespace access scoping; if the same KV namespace is later reused by another route/binding, cross-feature cache key collisions become an IDOR-adjacent risk. | Use a distinct, namespaced key prefix (e.g. `xref:v2:`) so future features can't collide with or overwrite this cache's entries. |
| SEC-10 | low | Reference validation (`parseReference`, unknown book/chapter → 404) | `BOOK_SLUGS` lookups and `resolveReference`'s `case 'unknown'` fall through to `getVerseById`/`getVerseByReference` with raw user input as a parameterized value — fine for injection, but plan doesn't bound input length before it reaches Postgres. | Add a max-length check (e.g. 100 chars) on the `:book`/`:chapter` route params before querying, to avoid pathological/oversized inputs. |

| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|----|----------|-------|------------------------|-------------------|
| PERF-1 | high | Home loader: `getVolumeList` then `Promise.all(volumes.map(v => getBooksByVolume(db, v.id)))` | `getBooksByVolume` (packages/scripture/src/queries.ts) issues a JOIN query, and on empty result a second fallback query — up to 10 sequential Hyperdrive round trips fired in parallel on every `/` hit, uncached. | Add a single `getBooksByVolume` variant that fetches all volumes' books in one grouped query (`WHERE volume_id IN (...)`), or cache the home page shape in KV despite "PG uncached" scope note. |
| PERF-2 | high | Plan §KV cache scope: "KV cache only for Neo4j reads; PG reads uncached in this phase" | Home loader's 5-10 PG round trips run on every request with no cache layer, against a 10ms CPU budget (free tier) and Hyperdrive's own connection-pool latency per query. | Reconsider scope: cache home page volume/book list in KV (data is near-static, same rationale as the 7-day Neo4j TTL) even in this phase. |
| PERF-3 | med | `packages/neo4j-http/src/client.ts` `execute()` — `AbortSignal.timeout(20_000)` | A 20s timeout on Neo4j Aura's cold-resume path (60s+) means the loader can hold a Worker invocation open far past typical UX budgets before the plan's degradation kicks in, wasting wall-clock/CPU billing. | Lower timeout for the interactive path (e.g. 3-5s) with degradation firing fast; reserve long timeouts for background/ingest calls only. |
| PERF-4 | med | Scripture loader: `getVersesByChapter` + `getChapterSummary` in parallel, verse payload | Psalm 119 (176 verses) ships as one uncached JSON payload with `reference` and `text` per verse on every request; plan explicitly excludes PG reads from caching. | Cache rendered chapter payload (verses+summary) in KV like cross-refs; scripture text is as immutable as the graph the plan already caches. |
| PERF-5 | med | `packages/neo4j-http/src/client.ts` `createNeo4jClient` — plan: "Per-request Neo4j client factory (stateless)" | Client is cheap to construct, but each `execute()` opens a fresh HTTPS connection per request (no keep-alive/connection reuse across requests) to Aura, adding TLS handshake latency on every cross-ref lookup that isn't a cache hit. | Confirm `fetch` on Workers reuses HTTP/2 connections to the same origin; if not, document expected added latency (~100-300ms) per cold cross-ref request. |
| PERF-6 | med | Scripture loader `?verse=N` path: `cachedJson` wraps `findCrossReferences` | Cache key isn't specified in the plan; if keyed only on `verse_id` without a namespace/version prefix, KV's 1-write/sec/key limit could be hit under concurrent first-time requests for a popular verse (e.g. John 3:16) thundering-herd on cache miss. | Document cache key scheme; accept duplicate Neo4j calls on concurrent miss (no lock) since KV can't do request coalescing — note this as accepted risk, not a bug. |
| PERF-7 | low | `getBooksByVolume` (packages/scripture/src/queries.ts) primary query | JOIN of `lumen.verses` to `lumen.entities` with `DISTINCT e.id, ...` and `ORDER BY e.id` scans all verse rows for a volume (up to thousands) to derive a book list, when book set could come from `entities` alone. | Query `lumen.entities WHERE entity_type = 'book' AND volume_id = ...` directly if that FK exists, avoiding the verses-table scan on the hot home-page path. |
| PERF-8 | low | Security headers middleware (plan: "Security headers on all responses") | No mention of where headers are applied (per-response wrap vs. per-route) — if implemented as a loader-level concern rather than a single response-transform in `workers/app.ts`, risks duplicated header-setting logic and missed routes as more routes are added. | Apply headers once via a top-level `handleRequest`/`entry.server.tsx` response wrapper, not per-loader, to keep it O(1) and future-proof. |
| PERF-9 | low | Module-scoped postgres singleton, `db.server.test.ts` retry semantics | Retry-on-init-failure pattern (catch→store→re-throw) is correct for correctness, but plan doesn't address what happens to the *first* Worker isolate's failed connection state — Hyperdrive connection churn under a burst of cold starts each attempting first-request construction. | Note expected behavior under cold-start storms (each new isolate pays fresh Hyperdrive handshake); no code change needed, just document as accepted cost. |
| PERF-10 | low | Plan context: "Neo4j Aura free tier... first query after resume can take 60s+ or fail" combined with degradation only on `?verse=N` path | Chapter loader itself (no `?verse`) never touches Neo4j per plan, which is good, but the *first* user to add `?verse=N` after 72h idle eats the full cold-resume latency/timeout before degrading — no warmup/ping mechanism mentioned. | Consider a cheap scheduled Worker (cron trigger) to ping Neo4j periodically and prevent Aura auto-pause, avoiding user-facing cold-resume stalls. |

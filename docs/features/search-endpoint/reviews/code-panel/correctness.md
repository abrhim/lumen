# Code-panel — correctness

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| CORC-1 | high | apps/web/app/routes/api.search.tsx:74-87 | Session/visibility derivation (getSessionUser, getPublicCollectionIds, getEntitlements, collections query) sits outside try/catch; any error escapes the loader as a framework-generic non-JSON 500 without code:'internal', Cache-Control, or search_failed log. | Move visibility derivation inside the existing try (or its own try) so failures return the decision-5 JSON 500 and log search_failed. |
| CORC-2 | med | packages/scripture/src/search.ts:497 | coerceRow runs unprotected in the fallback path; one double-encoded non-JSON payload row (known corruption class; wordsLeg passes si.payload raw) throws JSON.parse in both modes, 500ing the whole search — violating decision 7. | Wrap per-group row coercion in try/catch; degrade that group to results:[] plus meta.error instead of throwing. |
| CORC-3 | med | packages/scripture/src/search.ts:296-313 | episodesLeg WHERE omits the trgm predicate its tier/score CASEs reference (dead code), and % full-string prefilter can never pass on long titles — fuzzy episode-title recall is entirely nonfunctional; artLeg omits trgm too. | Add a word_similarity-based predicate on si.title to WHERE (served by idx_search_title_trgm), or delete dead CASE branches and ratify the deviation. |
| CORC-4 | med | apps/web/app/routes/api.search.tsx:59 | scope CSV order is forwarded verbatim and searchAll maps it directly; scope=words,scripture returns groups [words,scripture], violating decision 5's 'scoped keys in GROUP_KEYS order'. Tests only use canonical-order scopes. | Canonicalize: scope = GROUP_KEYS.filter(k => requested.has(k)) before calling searchAll; pin with a reversed-scope test. |
| CORC-5 | med | packages/scripture/src/search.ts:449-457 | resolveSearchReference has no error isolation: a transient DB error on the chapters/verses probe rejects searchAll and 500s reference-shaped queries, while leg failures are double-isolated — inconsistent partial-failure behavior. | try/catch around resolveSearchReference; on failure treat as reference:null and proceed to FTS legs. |
| CORC-6 | med | scripts/migrate-search-projections.mjs:128-131 | Invariant moments_and_episodes_untouched hardcodes 3940 moments/10 episodes; M4 re-runs on the post-ingest runbook, so after episode 11 it falsely exits 2 after already committing. | Capture baseline counts at run start and compare before/after (as M3's ownership check does), not literal counts. |
| CORC-7 | low | scripts/build-search-moments.mjs:50-53 | HARD_MAX is checked after appending a whole caption, so windows exceed the plan's 800 hard cap unboundedly (probed 3018 and 1305 chars); tail-merge also overshoots (815). | Ratify the chain-over-cap trade-off in plan.md, or flush before appending when overflow is predictable; cap is unenforceable without splitting captions. |
| CORC-8 | low | packages/scripture/src/search.ts:431 | JS id tiebreak uses code-unit order but legs' ORDER BY id uses en_US.UTF-8 (probed divergence: 'a#2' vs 'a-b'); tier-2 score-0 ties are common, so LIMIT membership and served order use different id orders. | Add COLLATE "C" to each leg's ORDER BY id so SQL matches the JS comparator; still deterministic either way. |
| CORC-9 | low | apps/web/app/routes/api.search.tsx:65-70 | limit= (empty string) coerces via Number('')=0 to clamped 1 instead of 400 or default 8; '0x10' is accepted as 16 — edges of the 'non-numeric limit → 400' contract. | Treat empty/whitespace rawLimit as absent (default 8) and validate with /^\d+$/ before Number(). |

## Evidence

All probes ran read-only against live prod as lumen_read (DSN from apps/web/.env), executing the actual worktree searchAll via tsx.

PROBE A (searchAll live): q=faith → meta.mode='combined', all 7 groups populated (scripture:8 people:8 places:4 topics:8 episodes:8 art:8 words:8) — combined statement is valid and is the live path; single-leg scope (art) also runs combined. Rules out combined-loop/legs[0] defects.

PROBE B (CORC-4): searchAll(scope:['words','scripture']) → groups order ["words","scripture"], mode=combined — user order served, not GROUP_KEYS order; decision 5 says "groups always contains exactly the scoped keys in GROUP_KEYS order". Route (api.search.tsx:59) forwards [...new Set(parts)] verbatim; search.ts:441/444 maps it directly.

PROBE C (CORC-3): For the Leviticus episode title: word_similarity('leviticas', title)=0.6999 (≥0.45 threshold) but '%'-operator=false, fts=false, prefix=false → searchAll(q='halvorsen', scope:['episodes'])=0 hits and 'leviticas' cannot reach the episode by ANY channel. episodesLeg WHERE (search.ts:311-313) has no trgm predicate while tier CASE (298-299) and score (304-305) test it — dead branches. M1 created idx_search_title_trgm (pg_stat shows idx_scan=3, essentially unused) for a predicate never issued. Entity-side %+word_similarity form is ratified by amendment A1; the episodes WHERE omission is not.

PROBE D (reference short-circuit): 'dc' → {level:'volume', book_id:'dc', found:true} (dual-namespace carried correctly); 'nt' → volume without book_id; verse id 'dc-76-22' exists so D&C verse short-circuit resolves. No finding.

PROBE E (CORC-8): SELECT unnest(ARRAY['a-b','ab','a#2','a2']) ORDER BY x under prod datcollate=en_US.UTF-8 → ['a-b','a#2','a2','ab']; JS .sort() → ['a#2','a-b','a2','ab']. Ties are real: 10 'mel%' persons match ILIKE-prefix with zero FTS rank → identical tier-2 score 0, id decides both SQL LIMIT membership and JS page order. Moment ids contain '#'.

PROBE F (windowCaptions, pure fn run locally): [short, 3000-char caption, short] → single window len=3018; 499+299+500 captions → window len=1305; tail-merge → 815. Plan decision 8 claims bounds 200-800. Empty array → [] (unreachable in main: episodes selected via EXISTS transcripts); single caption → one window, chain intact; all-null t_end_s → window t_end_s=null (prod currently has 0 null t_end_s transcripts/moments, and coerceRow passes null through untouched).

PROBE G (payload integrity, CORC-2 context): jsonb_typeof(payload)<>'object' count = 0 across all kinds today (moment 3940, episode 10, strongs 20734, artwork 4461) — CORC-2 is latent, but the double-encoded-string class occurred before (the 10 episode payloads M3 repaired); wordsLeg passes si.payload through raw (search.ts:359), and fallback-path coercion at search.ts:497 sits outside Promise.allSettled/try, so one bad row rejects searchAll.

Static verifications: chapter_summary ids 1582/1582 conform to '^summary-.+-[0-9]+$' (payload regex safe); jst_reading 31262/31262 carry metadata verse_id (JST payload safe); GROUP_RESULT_TYPES exactly matches leg outputs (entityLeg type whitelist = its CASE domain); anyOf('') → NULL → fail-closed confirmed by H8 green; clampLimit handles NaN/negatives. CORC-1 is structural: api.search.tsx lines 74-87 (4 awaits: session, publicIds, entitlements, collections) precede the try at line 89; an exception there bypasses json()/logEvent entirely — React Router resource routes return a generic non-JSON 500. Known recurring trigger in this project: Supabase session-pool exhaustion (pool cap 15).

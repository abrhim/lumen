# Code-panel — api-contract

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| APIC-1 | high | apps/web/app/routes/api.search.tsx:59 | scope keys forwarded in user-supplied order; searchAll (search.ts:444) maps them verbatim, so ?scope=words,people returns groups out of GROUP_KEYS order — violates decision 5's documented MUST. | Canonicalize: scope = GROUP_KEYS.filter(k => requested.has(k)) in the loader (or in searchAll); add a non-canonical-order scope pin to H14. |
| APIC-2 | med | apps/web/app/routes/api.search.tsx:74-87 | Session/visibility awaits run before the try block; any rejection yields a framework 500 with no Cache-Control: private,no-store, no {error,code:'internal'} JSON, and no search_failed log. | Move getSessionUser/getPublicCollectionIds/getEntitlements/collections query inside the existing try so every 500 flows through json() and logEvent. |
| APIC-3 | med | packages/scripture/src/search.ts:210 | Verses matched only via kjv_delta (believe→believeth, the feature's headline case) return snippets with zero ⟪⟫ markers — ts_headline runs the modern query against the archaic text (live-verified). | Expand the headline tsquery with reverse kjv-variant forms (modern→variants) for scripture snippets, or amend the contract to state markers are best-effort. |
| APIC-4 | med | packages/scripture/src/search.ts:497 | Fallback path runs coerceRow after allSettled; one JSON.parse-failing payload (the historical double-encoded class) throws from searchAll → endpoint 500, breaking decision 7's 'never 500 from a group failure'. | Move coerceRow+sortResults inside the per-leg async so a poisoned group degrades to results:[] + meta.error like any other group failure. |
| APIC-5 | low | apps/web/app/routes/api.search.tsx:66-70 | ?limit= and ?limit=%20 coerce via Number('')=0 and silently clamp to 1 (not 400, not default 8); ?limit=3.5 400s though decision 5 clamps numeric out-of-range input. | Treat empty/whitespace rawLimit as limit_invalid; floor finite numerics then clamp (clampLimit already floors); pin both edges in H14. |
| APIC-6 | low | packages/scripture/src/search.ts:69 | Decision 5 requires documenting id stability (all durable EXCEPT response-scoped moment ids); shipped code/docs contain no such note — moment ids silently change on every M3 re-window. | Add a doc comment on SearchResult.id (and route header comment): moment ids are response-scoped; deep-link via payload episode_id/t_start_s. |

## Evidence

PROBES (prod, lumen_read, node -e + pg, SELECT-only):
[1] Snippet-marker probe — ts_headline over a 'believeth' verse text with q='believe' (the H1 flagship delta match) returns NO markers: "He that believeth on the Son hath everlasting life: and he that believeth not the" (no ⟪⟫); control q='faith' over matching text returns "⟪faith⟫ is the substance of things hoped". So every verse reached ONLY via kjv_delta (believe/spoke/show class — the feature's Gap-1 raison d'être) ships a snippet with zero highlight markers.
[2] Payload MUST-field probes — all CLEAN (non-findings): chapter_summary id format violations n=0 of 1582; extraction mismatch vs lumen.chapters bad=0; sample summary-1-chr-5 → book_id='1-chr', chapter=5 (greedy regex correct). artwork total=4461, null_thumb=0, no_refs=0. strongs total=20734, payload strongs_no≠ref_id bad=0. moment total=3940, missing episode_id/t_start_s/t_end_s=0. episode payloads are objects keyed episode_id (M3 repair held). jsonb_typeof(payload)<>'object' → zero rows, all kinds. jst_reading metadata verse_id nulls=0 of 31262.
[3] Groups-order trace: api.search.tsx:55-59 builds scope as [...new Set(rawScope.split(','))] preserving USER order; search.ts:441-444 does groups=scope.map(...) with no canonical reorder. GET ?scope=words,people returns groups keyed [words,people], but plan decision 5 says "groups always contains exactly the scoped keys in GROUP_KEYS order". The only ordering test (api-search.test.ts:101-107) omits the scope param; the scope test (:96-99) feeds canonical-order input — the violation is untested. Not ratified by amendments A1–A5.
[4] Error-path trace: awaits at api.search.tsx:74 (getSessionUser), :75 (getPublicCollectionIds), :79 (getEntitlements), :81-83 (collections query) sit OUTSIDE the try{} that starts at :89. A rejection there escapes the loader → framework default 500; json() (:24-33), the only place Cache-Control: private,no-store and {error,code:'internal'} are produced, never runs; search_failed (:128) never logs. H18 tests only cover searchAll rejection (mocked), not these.
[5] Isolation trace: fallback path applies coerceRow at search.ts:497 AFTER Promise.allSettled resolves — a payload that JSON.parse-fails (the historical double-encoded class M3 repaired: 10 prod rows) throws synchronously from searchAll → route 500, violating decision 7 "never 500 from a group failure". Combined path hits the same row inside try (:471-472) → silently falls back → re-runs all legs → same throw at :497.
[6] limit edges: Number('')=0 and Number(' ')=0 pass Number.isInteger → ?limit= / ?limit=%20 silently clamp to 1 (not 400 limit_invalid, not default 8); Number('3.5') fails isInteger → 400, though decision 5 says only non-numeric→400 and out-of-range→clamped (clampLimit even has the unused Math.floor). H14 tests only limit=abc/999/0.
[7] Moment-id doc: grep for stab/durable/response-scoped over search.ts + api.search.tsx finds only "stable codes" comment; SearchResult.id (search.ts:69) and ref_id='ep#seq_start' carry no note that moment ids are response-scoped (they change on every M3 re-window), despite decision 5's "Id stability documented" MUST.

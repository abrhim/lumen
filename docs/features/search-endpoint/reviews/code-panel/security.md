# Code-panel — security

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| SECC-1 | med | apps/web/app/routes/api.search.tsx:74-87 | Visibility derivation (getPublicCollectionIds, admin collections query) runs outside the try/catch; a DB failure there throws an unhandled loader error — bypassing the scrubbed JSON 500, Cache-Control: private,no-store, the search_failed log, and dropping session-rotation Set-Cookie headers. | Move lines 74–87 inside the existing try so any failure exits via the json({code:'internal'},500,headers) path. |
| SECC-2 | low | packages/scripture/src/search.ts:359 (also 333) | wordsLeg forwards full si.payload (lang, original, translit, gloss; contract says {strongs_no}); artLeg keeps artist_name/year beyond {refs,thumbnail_url}. Passthrough means any future projection-script field ships to clients automatically. | Build payloads with jsonb_build_object allowlists (as scripture/topics legs do) instead of payload minus-key passthrough. |
| SECC-3 | low | packages/scripture/src/__tests__/search-harness.test.ts:37 | SEC-6 role assertion is prefix regex /^lumen_read/ — a writable role named e.g. lumen_readwrite or lumen_read_admin would satisfy the write-backstop check. | Assert exact equality: expect(who[0].current_user).toBe('lumen_read'). |
| SECC-4 | low | apps/web/app/routes/__tests__/api-search.test.ts:178 | The admin authz branch (ADMIN_COLLECTIONS entitlement → all collections, visibility:'admin' + userId logging) has zero test coverage; only anonymous and non-entitled paths are pinned. | Add a pin: mock entitlement rows + collections query, assert visibleCollections = all ids and search_executed logs visibility:'admin'. |
| SECC-5 | low | scripts/build-search-moments.mjs:179; scripts/migrate-search-projections.mjs:93 | Collection-stamping invariants (and harness H7b) cover moment/artwork/strongs but not kind='episode' (ingest-owned); a future unstamped episode row fails closed but silently vanishes from search, uncaught. | Extend the stamping invariant and H7b to all search_index kinds including 'episode'. |

## Evidence

All probes ran LIVE against prod as lumen_read (current_user verified). (1) Gate composition — EXPLAIN of the exact entityLeg predicate shape shows the visibility gate applied as an index condition BEFORE any OR branch: "Recheck Cond: ((entity_type = ANY('{person}')) AND (collection_id = ANY('{phase-b}'))) … BitmapAnd(idx_entities_type, idx_entities_collection); Filter: (lower(name)='melchizedek' OR name ~~* 'melchizedek%' OR (OPERATOR(extensions.%) …>=0.45) OR search_vector @@ …)" — no tier-1 exact-match bypass is possible; same AND-outside-OR shape confirmed by reading all five legs (JST sub-query gated at search.ts:227). (2) Fail-closed anyOf — tier-1 exact 'melchizedek' with visible='' → string_to_array(NULLIF('',''),',')=NULL → 0 rows; visible='phase-b' → 1 row. Episodes leg with unshaken removed from the CSV → 0 rows for 'Halverson' (kill switch works for search). (3) A4 premise verified: lumen.collections has 0 ids containing ','; 8 collections, all public=true (matches deliberate 2026-07-21 decision — not a finding). (4) escapeLike byte-level: ILIKE 'z\%%' ESCAPE '\' → 0 vs unescaped 'z%%' → 196; 'a\_%' → 0 vs 'a_%' → 1599; grep confirms all 6 ILIKE sites use the escaped prefix param; ESCAPE '\\' in the TS template literal renders as SQL ESCAPE '\' (single backslash) — correct. (5) Admin path: getSessionUser does local ES256 JWKS verification and never throws; getEntitlements is fail-closed (empty Set on any error) and reads lumen.user_roles⨝roles (no JWT claims); prod user_roles is EMPTY (admin role: 0 holders) so the admin branch is currently dormant — a non-admin cannot reach it (inverting has() would be caught by the existing non-entitled test since the mocked collections query returns []). (6) Payload exposure: moment payload post-strip = {episode_id, t_start_s, t_end_s} — transcript text correctly stripped (snippet comes gated via ts_headline); strongs payload = {lang, gloss, original, translit, strongs_no} and art payload keeps artist_name/year/refs/thumbnail_url → SECC-2 (benign public data today; contract says {strongs_no}/{refs,thumbnail_url}). (7) search_index stamping: artwork→art (4461), episode→unshaken (10), moment→unshaken (3940), strongs→strongs (20734); zero NULLs today but the invariant set omits kind='episode' → SECC-5. (8) 500 path: response body is {query, reference, groups} only — per-group meta.error (raw driver messages) never leaves the server; error scrub pinned in api-search.test.ts:123-137. But the try block starts at api.search.tsx:89, after getPublicCollectionIds (line 75, throws on DB error — queries.ts:172 has no catch) and the admin collections db.execute (line 81) → SECC-1; realistic trigger: Supabase session-pool exhaustion (documented prior incident in this repo). (9) M1 grants are narrowly scoped (USAGE on extensions + EXECUTE on 6 named functions to lumen_read); no secrets/DSNs in the diff; scripts scrub error messages via scrubSecrets. Ratified deviations (A1 % operator, A4 comma-join mechanism, public unshaken, no rate limiting) excluded as findings per plan.

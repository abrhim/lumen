# Code-panel — test-harness-quality

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| TESC-1 | high | packages/scripture/src/__tests__/search-harness.test.ts:291 | No test pins meta.mode==='combined'. searchAll's bare catch (search.ts:481) silently falls back, so a permanently broken combined statement — the PER-3 primary path — passes all 40 tests while every request runs 7 serialized round trips. | Assert res.meta.mode==='combined' in H10/H12/H13; optionally record combined-failure reason in meta and pin its absence. |
| TESC-2 | med | packages/scripture/src/__tests__/search-harness.test.ts:298 | H13 claims payload typing 'across ALL kinds' but 'millennial reign' live returns only moment/person/principle/summary/symbol/topic; verse, jst, episode, artwork, strongs payload coercion is never asserted; sawMoment rests on 2 live rows. | Add per-kind queries (faith→verse/episode, JST Genesis→jst, pentecost→artwork, agape→strongs) asserting typeof payload and numeric fields per kind. |
| TESC-3 | med | apps/web/app/routes/__tests__/api-search.test.ts:38 | EMPTY_RESPONSE drifts from real SearchResponse (missing meta.mode, which the loader logs at api.search.tsx:116); the untyped mock hides drift, and the zero-hit group-order test (line 101) only asserts mock passthrough — real searchAll's exact-GROUP_KEYS-order contract is unpinned everywhere. | Type the mock `satisfies SearchResponse`; add a live pin that searchAll groups map exactly equals GROUP_KEYS order (and per-scope subset). |
| TESC-4 | med | packages/scripture/src/__tests__/search-harness.test.ts:332 | H17 asserts only poisoned>0, scripture>0, and SOME meta error — it never pins mode==='fallback', that all three poisoned groups (people/places/topics) are empty WITH error, or that episodes/art/words survive (live: 8/8/4 hits). | Assert mode==='fallback'; assert perGroup people/places/topics each {hits:0,error}; assert episodes/art/words hits>0. |
| TESC-5 | med | apps/web/app/routes/__tests__/api-search.test.ts:87 | H14 misses the Number('')===0 trap: ?limit= (and limit=%20) silently clamps to 1 instead of 400 or default 8 (api.search.tsx:66); limit=2.5 400-path and whitespace-only q are also unpinned. | Pin limit=, limit=%20, limit=2.5, q=whitespace; treat empty/whitespace rawLimit as limit_invalid or unset. |
| TESC-6 | med | apps/web/app/routes/api.search.tsx:78 | The admin-entitlement branch (ADMIN_COLLECTIONS → all collections, visibility='admin', userId logged) has zero test coverage — the loader's only privilege-escalation path is unasserted. | Add endpoint test mocking entitlements with ADMIN_COLLECTIONS and db returning collection rows; assert visibleCollections=all and visibility:'admin'+userId in search_executed. |
| TESC-7 | low | apps/web/app/routes/__tests__/api-search.test.ts:141 | search_executed is asserted by event name only — no OBS-1 payload fields pinned (mode logs undefined under the drifted mock, unnoticed); search_group_degraded (OBS-2) is never exercised at the endpoint. | Assert search_executed payload keys/values (mode, zeroResult, perGroupMs, visibility); feed degraded meta through the mock and pin search_group_degraded. |

## Evidence

All probes ran live against prod as lumen_read (DSN from apps/web/.env), read-only. (1) H8 premise VERIFIED GENUINE — not a finding: SELECT websearch_to_tsquery('english','JST Genesis') = 'jst' & 'genesi'; verse_hits=0, jst_hits=1732 (lumen.entities entity_type='jst_reading' via search_vector @@). JST leg really matches via FTS; no verse crowding possible at limit 8. (2) H13 probe: for 'millennial reign' — moment_hits=2, episode_hits=0, artwork_hits=0, strongs_hits=0, verse_hits=0, jst_hits=0; moment payload t_start_s is jsonb number ("469.615"/"11400.654", jsonb_typeof=number). Live searchAll replay: types-seen=["moment","person","principle","summary","symbol","topic"], perGroup hits {scripture:0,people:1,places:0,topics:8,episodes:2,art:0,words:0} → verse/jst/episode/artwork/strongs payload coercion never exercised by H13; sawMoment rests on 2 rows. (3) Combined path: live searchAll(q='faith') → meta.mode='combined', hits {scripture:8,people:8,places:4,topics:8,episodes:8,art:8,words:8} — works today, but grep confirms no test asserts meta.mode anywhere; search.ts:481-483 is a bare `catch {}` so a broken combined statement is invisible to the suite (only symptom: 7 serialized round trips; plan itself measured 415ms for 7x RTT). (4) H17 mechanism VERIFIED GENUINE: JSON.stringify(sql`...entity_degree...`) contains 'entity_degree' (drizzle chunks serialize raw SQL); live poisoned replay → poisoned=4 (combined + people + places + topics legs), mode='fallback', perGroup {scripture:{8,ok},people:{0,err},places:{0,err},topics:{0,err},episodes:{8,ok},art:{8,ok},words:{4,ok}} — behavior is correct but the test pins almost none of it (also note brittle coupling to drizzle-internal serialization, worth a comment in the test). (5) Endpoint mock drift: api-search.test.ts:38-43 EMPTY_RESPONSE has meta {perGroup,totalMs} but real SearchMeta (search.ts:100-104) requires mode; api.search.tsx:116 logs `mode: result.meta.mode` → undefined in every endpoint test, and the search_executed test (lines 139-143) checks only the event name. Zero-hit test (101-107) asserts the mock's own groups round-tripped — tautology; live harness never asserts groups===GROUP_KEYS order (H8 closed-leg loop and H11 `every` are vacuous if keys were missing). (6) H14 code-level: api.search.tsx:66-70 — Number('')===0 passes Number.isInteger → `?q=faith&limit=` clamps to 1 (not 400/not default 8); untested. H14 error codes q_required/q_length/scope_unknown/limit_invalid/internal ARE all pinned, and the clamp trio (999→25, 0→1, default 8) IS pinned (api-search.test.ts:87-94) — those plan promises are met. (7) Admin branch api.search.tsx:78-87 (getEntitlements→ADMIN_COLLECTIONS→SELECT id FROM lumen.collections, visibility='admin') has no test; the two wiring pins cover only anonymous and non-entitled users. Non-findings verified: H9 'z%' pin logic sound (escapeLike → ILIKE 'z\%%'); harness DSN path resolves to repo-root apps/web/.env; current_user pin enforces lumen_read; H12 latency and H2 live floors are plan-ratified (decision 7 / PER-5), not flagged.

# Code-panel — observability

> Salvaged from killed run wf_bd88ca74-72d (2026-07-21 15:35; this reviewer completed before the session died). Agent transcript: session 4a3e5fa5, agent-a715fe28c5567d674.

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| OBSC-1 | high | apps/web/app/routes/api.search.tsx:74-87 | Session/visibility phase (getSessionUser, getPublicCollectionIds, getEntitlements, admin collections query) is outside the try — failures 500 without search_failed, JSON {error,code}, or Cache-Control: private,no-store. | Widen the try (or add a wrapper catch) so visibility-phase errors return the same 500 JSON contract and emit search_failed. |
| OBSC-2 | med | packages/scripture/src/search.ts:481-483 | Bare catch {} swallows the combined-statement error. When fallback legs all succeed (live-proven combined-only class, e.g. UNION type collision), no event fires; every request silently double-executes. | Capture the error into meta (e.g. combinedError); loader logs a degraded event with the message when mode==='fallback'. |
| OBSC-3 | med | apps/web/app/routes/api.search.tsx:128-131 | search_failed logs only {message, qLen} — the failing q/scope/visibility are dropped, making 500s unreproducible; no privacy gain since search_executed logs raw q on every success. | Include q, scope, and visibility in the search_failed payload. |
| OBSC-4 | low | apps/web/app/routes/api.search.tsx:113 | zeroResult counts degraded groups as zero hits — a fully-degraded fallback request logs zeroResult:true, contaminating the relevance metric, contradicting the OBS-2 comment at line 104. | Exclude requests with any perGroup.error from zeroResult, or add a degradedGroups count field to search_executed. |
| OBSC-5 | low | packages/scripture/src/search.ts:500-503 | Degraded groups always report ms:0 (rejection loses the leg timer), so search_group_degraded cannot distinguish instant failures (missing relation) from timeouts — the one field decision 10 specified. | Time each leg inside a caught wrapper so the rejection carries elapsed ms into meta and the degraded event. |
| OBSC-6 | low | packages/scripture/src/search.ts:470-476 | Combined-mode perGroup.ms stamps every group with whole-searchAll elapsed (t0 at line 438, includes reference-resolution round trips) — not group time; semantics undocumented, so perGroupMs dashboards misattribute. | Document on SearchGroupMeta.ms; in combined mode omit or null perGroupMs in the log, keeping elapsedMs authoritative. |
| OBSC-7 | low | scripts/build-search-moments.mjs:216-220 | M3 terminal events moments_done/moments_dry_run_ok (+ novel precondition_failed) deviate from the migration_done/migration_dry_run_ok vocabulary decision 10 says scripts follow verbatim; M3 is the recurring runbook step. | Rename terminal events to migration_done/migration_dry_run_ok (keep windows count field); fold precondition_failed into a precheck pass:false event. |
| OBSC-8 | low | scripts/migrate-search-kjv.mjs:189 | Dry-run emits migration_done {invariant_failures:0} without running invariants (same in migrate-search-projections.mjs:173); on real failure both exit 2 with no terminal event — unearned '0' in the durable log. | Follow the reference convention: run invariants in dry-run too and report the true failures count in migration_done. |
| OBSC-9 | low | apps/web/app/routes/__tests__/api-search.test.ts:42 | No test pins the loader's search_group_degraded emission (grep: zero hits in either suite), and the mock meta omits mode — OBS-1/OBS-2 field regressions pass green. | Add a pin: searchAll resolves with meta.perGroup error + mode:'fallback'; assert the degraded event and search_executed field set. |

## Evidence

LIVE PROBE (as lumen_read, prod pooler, SELECT-only) proving the combined-only failure class whose error search.ts:481's bare `catch {}` discards: legA `SELECT 1 AS x` → {x:1} OK; legB `SELECT 'moment' AS x` → {x:'moment'} OK; combined `SELECT * FROM ((SELECT 1 AS x) UNION ALL (SELECT 'moment' AS x)) f` → FAILED code 22P02 "invalid input syntax for type integer: \"moment\"". Both legs succeed standalone, so fallback fully succeeds → no meta.error → loader (api.search.tsx:100-107) emits zero search_group_degraded events; the only trace is mode:'fallback' with no message, while every request pays combined attempt + 7 serialized fallback statements. CODE TRACE: try-scope — api.search.tsx try opens at line 89; getSessionUser(74), getPublicCollectionIds(75), getEntitlements(79), admin collections query(81-83) are outside it; resource route has no ErrorBoundary, so a throw there bypasses json() (Cache-Control set only at line 31) and the search_failed catch (128). Reference-resolution failures DO reach search_failed (resolveSearchReference is called inside searchAll at search.ts:450, inside the loader try). Degraded ms: search.ts:500-503 sets {ms:0,hits:0,error} on rejection — the leg timer at 487 is lost — loader logs ms:0 (105). Combined perGroup.ms: t0 at search.ts:438, ms=Date.now()-t0 at 470 stamped identically per group (476), includes ref-resolution RTT. zeroResult (api.search.tsx:113) counts degraded groups' hits:0 as zero. SCRIPT VOCAB: reference migrate-media-collections.mjs runs invariants in dry-run and reports true count (lines 127-145: 'report, don't judge'; migration_done {invariant_failures: failures}); migrate-search-kjv.mjs:186-190 dry-run emits migration_done {invariant_failures:0} then returns before its invariant loop (222-233); on failures>0 exits 2 at 235 with no terminal event (projections identical at 197-198). build-search-moments.mjs emits moments_done(216)/moments_dry_run_ok(220)/precondition_failed(105) instead of migration_* vocabulary. TESTS: grep for search_group_degraded across apps/web/app/routes/__tests__ and packages/scripture/src/__tests__ → 0 hits; api-search.test.ts EMPTY_RESPONSE meta (line 42) = {perGroup:{}, totalMs:1} — no mode field; search-harness.test.ts H17 (318-340) pins searchAll-level meta.error only. search_executed (api.search.tsx:108-119) carries all decision-10 fields {q, scope, reference, groups, zeroResult, elapsedMs, perGroupMs, visibility} + mode + admin-only userId — completeness OK, cardinality bounded except plan-ratified raw q.

# Code-panel — data-integrity

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| DATC-1 | med | scripts/migrate-search-projections.mjs:129 | Invariant moments_and_episodes_untouched hardcodes moment=3940/episode=10. Next episode ingest + M3 changes counts; the mandated M4 re-run then exits 2 spuriously, training operators to ignore failures. | Snapshot non-owned kind counts before the tx and compare after, mirroring M3's dynamic baseline/after pattern. |
| DATC-2 | med | scripts/build-kjv-variants.mjs:137 | -edst class is entirely dead: 6 forms pass G3 live (deliveredst 5x, calledst 4x, commandedst 4x...) but first-match picks the past form ('delivered'), whose G2 siblings ('deliveredeth') can never attest; 0 shipped, no per-class gate catches it. | For -edst prefer the base candidate (slice(0,-4)) first or accept past-form targets in G2; assert per-class minimum counts in the eval gate. |
| DATC-3 | med | scripts/build-search-moments.mjs:139 | Deletes and the coverage invariant are both keyed off episodes still present in lumen.transcripts; when an episode is deleted (cascade), its kind='moment' rows persist forever, invisible to every check, and stay publicly searchable. | Add a kind-scoped delete of moments whose payload episode_id has no transcripts, plus a moment_orphan_free invariant mirroring M4's artwork_orphan_free. |
| DATC-4 | low | scripts/build-search-moments.mjs:52 | HARD_MAX checked after appending, so windows exceed the documented 200-800 bound: live 178/3940 moments >800 chars, max 1063; no length-bound invariant exists, so the deviation shipped unnoticed. | Flush before appending when the caption would exceed 800 (or ratify overshoot in plan) and add a bounds invariant. |
| DATC-5 | low | scripts/build-search-moments.mjs:156-160 | Payload repair overwrites the whole episode payload with {episode_id} without logging pre-repair contents or repaired rowcount; original double-encoded payloads are now unrecoverable except via re-ingest (BLA-1 capture discipline applied in M2 but not here). | Log pre-repair payload::text and the UPDATE rowcount before rewriting; preserve unknown keys where parseable. |
| DATC-6 | low | scripts/migrate-search-extensions.mjs:169-172 | The as-app-role functional probe exercises the pre-A1 predicate (set_config threshold + OPERATOR(extensions.<%)), not the ratified production form (% operator + word_similarity()>=0.45), so M1's own invariant no longer proves the deployed privilege path. | Update the probe to the A1 production predicate: OPERATOR(extensions.%) plus extensions.word_similarity(q,name) >= 0.45. |
| DATC-7 | low | scripts/build-kjv-variants.mjs:176-181 | Header claims curated classes are G1-checked, but ORTHOGRAPHIC/IRREGULARS bypass G1 entirely (attested.has(v) at any count, no target check): spake→spoke shipped with 'spoke' at n_lower=2. Behavior is desired; a typo'd curated target would ship unchecked. | Correct the header (curation supersedes G1) and add a cheap lexeme-existence sanity log for curated targets. |

## Evidence

All probes ran READ-ONLY as lumen_read (current_user verified) against prod via apps/web/.env DSN.

[DATC-1] search_index kinds live: artwork=4461, episode=10, moment=3940, strongs=20734 — invariant literal 3940/10 matches only today's state. M4 header (lines 5-6) + DAT-9 mandate re-runs on the same post-ingest runbook as M3, which changes moment counts by design.

[DATC-2] lumen.words probe — -edst forms with n_lower>=3 (pass G3): deliveredst=5, calledst=4, commandedst=4, desiredst=3, promisedst=3, trustedst=3. data/kjv-variants.json and lumen.kjv_variants (total=429): variant LIKE '%dst' count = 0. lumen.kjv_delta('he doeth what thou lovedst and commandedst') = '' (empty). Mechanism: edst branch cands=[slice(0,-2), slice(0,-4)] → 'delivered' attested wins first-match; G2 sibs from target 'delivered' = {delivereded, delivereding, deliveredeth, ...} → never attested → rejected. Eval logs a class histogram but asserts nothing per-class; no -edst pin in H16/pins.

[DATC-3] Live: 0 orphaned moment/episode rows today (NOT EXISTS transcripts probe empty), 0 moments with NULL payload episode_id — delete key verified sound for present episodes. But episodes list (lines 92-96) requires EXISTS transcripts and coverage invariant (lines 189-196) iterates FROM lumen.transcripts, so a deleted episode's moments escape both. collection_id stays 'unshaken' (public=true live) → content remains anonymously searchable after deletion.

[DATC-4] Live moment text lengths: n=3940, max=1063, >800: 178, >900: 39, <200: 0. Longest: unshaken-8SvK7L87o1A#669 (1063), unshaken-25hrVBU3Vz8#2541 (1059). shouldFlush evaluates cur.text.length AFTER append (windowCaptions lines 41-53); tail-merge (60-66) can push prev past 800 too.

[DATC-5] Live episode payloads, all 10: exactly {"episode_id": "unshaken-..."} — full replacement confirmed; script logs no pre-repair capture and no rowcount.

[DATC-7] lumen.words: spoke n_lower=2 (below MIN_LOWER=3); loved=115, said=4865, sitting=49; doe/goe/earn absent entirely, lowing=2 — so G1/G2 correctly killed doeth→doe, forest→for, lowest→low classes for generated morphology; G5 stopword drop confirmed for doth/does class ('doeth' 136x, 'doest' 48x attested but unmapped — ratified fail-safe).

Verified-clean (no finding): M2 trigger-first ordering is inside the tx (table+function+TRIGGER_DDL before commit, backfill after) and live pg_get_functiondef of both trigger functions byte-matches TRIGGER_DDL including kjv_delta legs and coalesce; VACUUM ANALYZE runs outside any tx (autocommit) — legal; backfill SET x=x re-run is idempotent and side-effect-free (only trg_verses/entities_search_vector triggers exist, tgenabled=O; zero updated/modified columns on either table). M3 repair UPDATE is inside the per-episode sql.begin. M4 DEGREE_REFRESH delete+insert IS inside sql.begin (lines 153-167) — transactional, contrary to the review-lens suspicion. M4 double-encoded metadata guard: live jsonb_typeof(metadata) for artwork = object x4461 (guard defensive-only), fame non-numeric count = 0. Collections live: all 8 public=true incl. 'strongs' (valid FK target; search_index has FK to collections + PK(kind,ref_id)).

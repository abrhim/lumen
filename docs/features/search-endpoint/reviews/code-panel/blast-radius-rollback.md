# Code-panel — blast-radius-rollback

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| BLAC-1 | med | apps/web/app/routes/api.search.tsx:74 | getSessionUser/getPublicCollectionIds/getEntitlements/collections-SELECT run outside the try (lines 74-87; try starts 89). First-touch DB failure yields a non-JSON 500 without Cache-Control:no-store or search_failed log — bypassing decision-5's error contract. | Widen the try to cover session+visibility work (or fail closed like main's getCollectionAccess .catch), so every 500 is JSON {error,code:'internal'} + no-store + logged. |
| BLAC-2 | med | apps/web/app/routes/api.search.tsx:75 | Worktree is 23 commits behind main; merged unshaken-surfaces added canonical collection-access.server.ts (getCollectionAccess/canViewCollection, fail-closed catch, DEV carve-out). Search hand-rolls a second visibility implementation — future kill-switch/entitlement changes will silently miss search. | Rebase onto main; derive visibleCollections via getCollectionAccess (plus admin all-collections expansion) so one module owns visibility. |
| BLAC-3 | low | apps/web/app/routes.ts:13 | Guaranteed merge conflict: this branch appends api/search last (BLA-7 discipline) but main's tail is now the :type/:id catch-all commented 'LAST on purpose' — two lines both claiming the last slot; merge-tree confirms conflict markers. | On rebase, place api/search above the :type/:id catch-all. (RR static-beats-dynamic ranking + node.tsx fail-closed loader make mis-resolution non-fatal, but keep the invariant.) |
| BLAC-4 | low | packages/scripture/src/search.ts:481 | Bare `catch {}` discards the combined-statement failure reason. A combined-only failure class (param-count/size limits, where every fallback leg succeeds) runs 1+7 statements per request forever with only mode:'fallback' logged — no cause, ever. | Capture the combined error into meta (e.g. meta.combinedError) and have the loader log it once alongside mode:'fallback'. |

## Evidence

LIVE PROBES (as lumen_read via apps/web/.env CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE; current_user=lumen_read — matches the app's runtime role):
1. M4-rollback degrade trigger verified at error-class level: SELECT from a missing lumen.* relation → code=42P01 severity=ERROR ("relation ... does not exist") — a pg client rejection, so the combined statement rejects into search.ts:481's catch, then Promise.allSettled isolates 42P01 to the 3 entity legs (LEFT JOIN lumen.entity_degree at search.ts:273); scripture/episodes/art/words legs reference no M4 object and survive. H17 (search-harness.test.ts:318-340) poisons exactly the entity_degree statements and pins: unaffected groups still return, failed group surfaces meta.error, no throw. Endpoint stays 200 (meta never reaches the client body). M2 rollback (drop kjv_delta/kjv_variants) touches nothing searchAll references at query time; M1 rollback degrades entity/episodes/words legs (OPERATOR(extensions.%)/unaccent) but scripture+art survive — consistent with the plan's reverse-order rule.
2. Deploy-forward state healthy: entity_degree=52,489 rows; kjv_delta('he believeth and goeth')='believe go'; search_index kinds artwork 4461/4461 stamped, episode 10/10, moment 3940/3940, strongs 20734/20734 (zero NULL collection_id — fail-closed ANY() holds); trgm probe 'melchisedek' %-matches 'Melchizedek' word_similarity=0.6 ≥ 0.45, unaccent('agapē')='agape' — all reachable as lumen_read with schema-qualified operators. New schema.ts defs match prod byte-for-byte: entity_degree(entity_id text, degree integer), kjv_variants(variant text, modern text).
3. slug-map guard blast radius: prod has chapters number<=0 = 0 and verses verse_number<=0 = 0 — the chapter>0/verse>0 guard can only reject parses that never resolved. Call sites: scripture.tsx:340/1386, scripture.art.tsx:22, book.tsx:16 (all URL-slug paths already carried the >0 guards at slug-map.ts:127/134, unchanged), resolve-reference.ts:23 (MCP resolve) — its 'unknown' branch (lines 80-92) falls back to getVerseById/getVerseByReference then a help message, strictly better than the old chapter-0 empty-verses response. Ratified by plan decision 12.
4. searchScriptures: git diff main...HEAD -- packages/scripture/src/queries.ts is EMPTY — Ring-2's only runtime consumer is byte-identical; DB-side effect is the ratified match-set-superset vector.
5. Ring-2 compilation: @lumen/scripture is "private": true, unpublished; only consumer in any repo on this machine is apps/web ("@lumen/scripture": "workspace:^"); docs/design/canon-spine.md:95 — "the MCP server consumes @lumen/scripture ... and deploys separately". So the MCP pins its own vendored copy: additive schema.ts defs + index.ts `export * from './search'` (search.ts imports only drizzle-orm, already a package dep) reach it only at its next redeploy — compile-safe, dead code until called. MCP redeploy is Out (deliberate).
6. Merge surface: merge-base 61dee0f, HEAD is 23 commits behind main (992c409). git merge-tree shows exactly two both-changed files: routes.ts CONFLICTS (`<<<<<<< .our  route("api/search"...)  =======  ...route(":type/:id","routes/node.tsx") LAST on purpose >>>>>>>`); .gitignore auto-merges cleanly (our !data/kjv-variants.json negation is at a different hunk than main's whisper-eval tail; the checked-in kjv-variants.json stays tracked). Main-side drift in shared surface since merge-base: new apps/web/app/lib/collection-access.server.ts + collection-display.ts + routes.ts (+6) — the source of BLAC-2/BLAC-3.
7. BLAC-1 code trace: api.search.tsx lines 74-87 (getSessionUser, getPublicCollectionIds, getEntitlements, SELECT id FROM lumen.collections) precede the try at line 89; a rejection there propagates out of the loader — react-router's default 500 (sanitized in prod builds, so no internals leak, but no JSON body/code, no Cache-Control: private,no-store, no search_failed logEvent, session refresh headers dropped). H18 only pins searchAll-rejection (api-search.test.ts:123). Contrast main's getCollectionAccess: getPublicCollectionIds(...).catch(() => []) fail-closed.

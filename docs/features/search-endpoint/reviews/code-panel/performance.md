# Code-panel — performance

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| PERC-1 | high | packages/scripture/src/search.ts:276 | Redundant non-indexable WHERE arm lower(e.name)=lower(q) forces heap-filtering the whole entity_type subset; idx_entities_name_trgm and idx_entities_search never serve the OR — A1's GIN-prefilter mechanism is inoperative. | Delete the equality arm from WHERE only (prefix-ILIKE arm subsumes it; keep tier CASE). Enables BitmapOr: 20.7ms→1.0ms people, 70ms→14.5ms topics; rows verified identical. |
| PERC-2 | med | packages/scripture/src/search.ts:338 | Art leg has the same pattern: lower(si.title)=lower(q) OR-arm forces pkey scan of all 4,461 artwork rows (3,534 buffers, 16.5ms) instead of BitmapOr on tsv+title_trgm GINs. | Drop the lower(title) equality arm from WHERE (ILIKE prefix arm subsumes it; tier CASE unchanged). Measured 3.8ms with identical rows. |
| PERC-3 | med | packages/scripture/src/search.ts:219 (verse arm; combined stmt :464) | Legal worst-case request (183-char OR-of-common-words, limit=25, all groups) runs 1,266ms server-side — 2.5x the 500ms p95 budget; verses seq-scan ranks 49,872 rows, topics leg 488ms. | Apply PERC-1/2 (cuts entity legs ~5x); cap websearch tsquery term count (e.g. numnode guard) or lower Q_MAX; watch decision-10 logs for tail. |
| PERC-4 | low | packages/scripture/src/search.ts:227,275 (collection_id = ANY) | With all 8 collections public, every entity leg BitmapAnds idx_entities_collection into a 65,887-row bitmap (~3ms, 159 buffers) — 4 legs/request of zero-selectivity work, both anonymous and admin. | Route already knows visibility: when visibleCollections covers every collection, pass a flag to omit the predicate on entities legs; retain it for restricted visibility (fail-closed unchanged). |
| PERC-5 | low | packages/scripture/src/search.ts:470-479 | Combined mode (the primary path) writes the whole-statement ms into every group's meta.perGroup, so OBS-1 perGroupMs — the stated per-group p95 instrument (PER-5/OBS-4) — logs 7 identical numbers. | In combined mode omit or null per-group ms (mode:'combined' is already logged); keep real per-group ms only on the fallback path. |

## Evidence

All probes read-only as lumen_read against prod (aws-1-us-west-2.pooler.supabase.com), EXPLAIN (ANALYZE, BUFFERS) on SQL mirroring search.ts legs; parameterized probe (probe2) confirmed custom plans const-fold websearch_to_tsquery (prepare:false production shape), so literal-inlined probes are plan-equivalent.

INVENTORY: verses 41,995; entities 65,887 (person 3,869, place 2,035, topics-set 8,380, jst_reading 31,262); search_index 29,145 (strongs 20,734, artwork 4,461, moment 3,940, episode 10); entity_degree 52,489. Indexes: idx_entities_name_trgm (gin_trgm_ops), idx_entities_search (GIN tsv), idx_entities_type, idx_entities_collection, NO index on lower(name); search_index: idx_search_tsv, idx_search_title_trgm, pkey(kind,ref_id), idx_search_coll. All 8 collections public=true.

PERC-1 (entity leg, OR-of-four): plan = BitmapAnd(idx_entities_type, idx_entities_collection) -> 'Filter: ((lower(name)='melchisedek') OR (name ~~* 'melchisedek%') OR (('melchisedek' OPERATOR(extensions.%) name) AND word_similarity>=0.45) OR (search_vector @@ tsquery))', Rows Removed by Filter: 3863 (people) / 7915 (topics, 58ms of 70ms in the heap-scan filter). Neither trgm nor FTS GIN used. Counterfactual (drop equality arm from WHERE only): BitmapOr(idx_entities_name_trgm[ILIKE], idx_entities_name_trgm[%], idx_entities_search) — people/melchisedek 20.658ms -> 1.035ms; topics/faith 69.954ms -> 14.536ms. Equivalence: tier:id sequences IDENTICAL for melchisedek, moses, JERUSALEM, jerusalem(places), faith, 'Faith, Trial of', lord (limit 25), since name ILIKE escape(q)||'%' subsumes lower(name)=lower(q).

PERC-2 (art leg): 'Index Scan using search_index_pkey ... kind=artwork; Filter: (... tsv @@ ... OR lower(title)=... OR title ~~* ...); Rows Removed: 3972; Buffers: shared hit=3534; 16.506ms'. No-eq variant: BitmapOr(idx_search_tsv, idx_search_title_trgm) 3.773ms; rows IDENTICAL (pentecost, 'The Last Supper').

PERC-3 (worst case in contract): q=183-char 'lord or god or ... or holy' (all words legal, len<=200), limit=25, 7-leg combined UNION ALL: Execution Time 1266.412ms (wallclock 1311-1318ms x3). Components: scripture 332ms (Seq Scan verses rows=28,832 + JST bitmap 21,040; Append ranked 49,872 rows), people 193ms, places 101ms, topics 488ms (4,842 degree-join loops), episodes 35ms, art 44ms, words 72ms. Typical combined (limit 8, warm, incl ~30ms RTT): lord 215ms (exec 184.9ms), faith 177-205ms, jerusalem 204-257ms, 'lord god' 202-213ms, believeth 162-164ms.

PERC-4: every entity leg shows 'Bitmap Index Scan on idx_entities_collection rows=65887, Buffers: 159, ~2.4-3.4ms' inside BitmapAnd — zero selectivity (all collections visible on 100% of anonymous and admin requests today); x4 legs (people/places/topics/JST) ~ 10-13ms/request.

PERC-5: search.ts combined path sets meta.perGroup[key] = {ms: <whole-statement ms>} for every key (same ms variable), then api.search.tsx logs perGroupMs from it.

VERIFIED-OK (no findings): ts_headline bounded to LIMIT rows — every leg plans as Subquery Scan over Limit-over-Sort; headline delta 0.1-3ms for 8 rows (e.g. topics 66.704 -> 69.865ms top node). Episodes leg BitmapOr(idx_search_tsv + pkey kind-arm) 9.943ms for 'lord' (1,370 tsv matches). Words leg BitmapOr(idx_search_tsv + pkey ref_id equality) 1.163ms for 'agape'. Boost LEFT JOIN = nested-loop on entity_degree_pkey, 5-9us/row (loops up to 4,842 -> ~20ms worst case). Verse leg 'lord': idx_verses_search bitmap, 8,840 matches, top-N heapsort, 59.756ms leg total incl JST arm (6,918 rows). Combined statement plans as plain Append of 7 independent subplans (per-leg sorts/limits preserved; parallel workers engage inside big legs). Planning time ~4.3ms/request (custom plans, prepare:false).

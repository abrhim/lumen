| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| OBS-1 | High | Scope §4 (verse panel loader) / Failure mode #8 | Cross-ref PG query moves into loader critical path but plan never states its failure behavior — degrade like `neo4j_degraded`, fall back empty, or 500 the page? | Add explicit handling: try/catch `getCrossReferences`, log `crossref_degraded`, resolve empty-with-flag like `loadConnections`, never let it 500 the chapter. |
| OBS-2 | High | Scope §2 (Ingest) | "unmapped-row count reported and capped" gives no log event name, no per-book breakdown, no capped sample — unlike `ingest-words.mjs`'s `zeroTokenVerses:{count,sample}` shape. | Emit named event `openbible_unmapped_refs` with count, ratio, per-book counts, capped sample (first 10 ids), mirroring existing house style. |
| OBS-3 | High | Scope §2 ("<0.5% or abort") | The 0.5% unmapped-ratio abort has no dedicated failure-mode entry, no named check, and no stated exit code — it's easy to implement as a silent early return. | Add FM-11: ratio ≥0.5% aborts with `openbible_unmapped_threshold_exceeded` log, exit code 1; harness-test the boundary value. |
| OBS-4 | Medium | Scope §2 (idempotent delete-then-insert) | Delete-then-insert per batch is specified but never logs delete vs insert counts, so idempotency on a re-run isn't verifiable from logs alone. | Log per-batch `openbible_batch_done {deleted, inserted, range}`, matching `words_batch_failed`'s verseRange shape. |
| OBS-5 | Medium | FM #10 vs Scope §5 (smoke-openbible.mjs) | FM #10 requires "smoke count stable across re-run" but the smoke script's bullet list (counts, orphans, spot checks, ordering, legacy path) omits any re-run/stability check. | Add explicit smoke bullet: re-run ingest, assert `lumen.edges` count for `openbible` collection unchanged. |
| OBS-6 | Medium | Scope §5 (famous-ref spot checks) | Only two spot-checked refs named for a ~600k-row corpus with 1,239 negative-vote and 655 cross-chapter-range rows; neither edge case gets a live check. | Add spot checks: a known negative-vote ref ranks last/below-fold; a cross-chapter range collapses to one card live. |
| OBS-7 | Medium | Scope §4 (panel) / client lens | Plan doesn't define how the panel signals zero cross-refs for a real Bible verse — visually identical to a silent query failure or ingest bug. | Render a distinct "no cross-references" state (not the degraded notice); log `crossref_empty` when count=0 to distinguish gap from bug. |
| OBS-8 | Low | Public contract (collection row) | Collection row only carries license/provenance strings, no ingest-run metadata (row count, ingested_at, source version) — no audit trail like canon-spine's `migration_state` marker. | Persist ingest summary (count, timestamp, source hash) on the collection row or `migration_state` for later audits. |

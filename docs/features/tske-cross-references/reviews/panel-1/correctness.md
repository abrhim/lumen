# Panel 1 — Correctness Review — tske-cross-references

| ID | Severity | Where in plan | Problem (≤25 words) | Fix (≤30 words) |
|---|---|---|---|---|
| COR-1 | Critical | FM-3, in-tx invariant (plan.md:106-107); smoke spot-checks (plan.md:71-72) | Invariant only checks the id *exists*; KJV vs. OpenBible off-by-one versification (Psalm titles, 3 John 14/15, Rev 12:17/18) can map to a real but *wrong* existing verse, silently, never counted as unmapped. | Add distribution/spot-check assertions for known-drift books (Psalm titles, 3 John, Rev 12) in smoke.mjs; can't rely on the unmapped cap alone. |
| COR-2 | High | Ingest, "Idempotent: delete-collection-then-insert in batched transactions" (plan.md:52-55); FM-10 (plan.md:119-120) | Batched (plural) transactions aren't atomic across the whole run; a mid-run abort (cap tripped or crash) leaves prod with a partially-deleted/partially-inserted `openbible` collection, serving broken Bible panels until re-run completes. | Wrap delete+all inserts in one transaction, or stage-then-atomic-swap; FM-10 should cover partial-failure visibility, not just re-run dedup. |
| COR-3 | High | Query layer direction (plan.md:57-58); panel labels "References"/"Referenced by" (plan.md:68-69) | OpenBible's from/to is canonical-spine order, not literal citation; framing it as directional "References"/"Referenced by" may misstate non-directional thematic pairs, and may not match legacy curated edges' from/to convention in the hybrid. | Confirm legacy edges share openbible's from/to convention before reusing one query fn; consider direction-neutral panel copy. |
| COR-4 | Medium | Scope item 1, range expansion (plan.md:43-45); osis-map.test.ts only covers same-book ranges | Plan cites 655 cross-chapter ranges but never states cross-book ranges were profiled as zero; `expandOsisRange(startRef, endRef, chapterId→verseCount)` has no path to roll into a new book's chapter list. | Explicitly profile/confirm zero cross-book ranges in source data, or extend the lookup contract to handle book rollover. |
| COR-5 | Medium | Unmapped cap "<0.5% or abort" (plan.md:55) | Threshold's denominator (345k source rows vs. ~600k expanded edges) and evaluation scope (per-batch vs. whole-run) are unspecified; a book-concentrated failure can hide under a global ratio or mis-trigger per-batch. | Define denominator and evaluate the cap once over the full run, not per insert batch. |
| COR-6 | Medium | Loader learning row (plan.md:36); item 4 (plan.md:63-66) | Plan says cross-refs move to the critical PG path but doesn't specify degrade-not-throw handling for that call; current `loadConnections` swallows Neo4j errors, new PG call in the awaited path could 500 the whole chapter on failure. | Give the new critical-path PG cross-ref fetch the same never-throw/degraded-result contract as `loadConnections`. |

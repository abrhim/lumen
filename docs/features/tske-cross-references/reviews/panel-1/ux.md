| ID | Severity | Where in plan | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| UX-1 | High | Scope §4 (verse panel swap) | Splitting cross-refs (critical-path) from principles/people (still streamed) makes chips pop in above already-rendered ref cards — layout shift the house rule forbids. | Reserve fixed-height slots for Principles/People (skeleton→content in place), or render all four groups from one resolved payload, not two async sources. |
| UX-2 | High | Q5 default (20/direction, "show all" deferred) | Section headers show only the rendered count ("References · 20"), not the true total; John 3:16 readers can't tell 20 is a fraction of hundreds. | Show "20 of 240" (or "+220 more") using a count query already available server-side; keep "show all" deferred, just disclose truncation. |
| UX-3 | Medium | Scope §4 ("ranges render as one card") | Undefined which verse a range card (e.g. "Ps 148:4–5") navigates/highlights to — likely range_start only, leaving verse 5 unhighlighted with no visual cue the ref spans further. | Specify: link to range_start, highlight the full span in the target chapter (multi-verse `bg-sel`), or show "4–5" in the destination panel title. |
| UX-4 | Medium | Q6 default ("curated (legacy)" chip) | "Legacy" reads as deprecated/broken to lay scripture readers unfamiliar with the software connotation, undermining trust in refs that are actually fine, just uncrossreffed by OpenBible. | Replace with neutral wording like "Curated" or "Editorial" chip; drop "legacy," which is an internal engineering term, not reader-facing. |
| UX-5 | Medium | Failure modes / empty states (not specified) | Same "No connections recorded" message covers a Bible verse with zero OpenBible refs (rare, looks like a bug) and a BoM verse with zero legacy refs (common, expected) — no differentiation. | Distinguish copy: Bible empty = neutral ("no cross-references found"); BoM/D&C empty = set expectation ("not yet curated for this volume"). |
| UX-6 | Low | Public contract (CC-BY footer credit) | Attribution sits at the panel's footer, below up to 40 ref cards in a scrollable rail/sheet — realistically never scrolled to, weak for a CC-BY visibility obligation. | Place a compact credit line directly under the References/Referenced-by section headers (or pin near panel top), not after all cards. |
| UX-7 | Low | Scope §4 (Cites/Cited by → References/Referenced by rename) | Renaming "Cites"/"Cited by" to "References"/"Referenced by" is undocumented as intentional; "Referenced by" is a less immediate directional cue than "Cited by" for a lay reader. | Confirm the rename is deliberate copy work, not incidental; if so, user-test the direction is still clear, e.g. keep "Cited by." |

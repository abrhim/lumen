# CODE-PANEL / SPECIALIST — performance review — canon-spine implementation

Reviewed: `spine-impl.diff`, `packages/scripture/src/queries.ts`,
`scripts/migrate-canon-spine.mjs`, `scripts/ingest-words.mjs`,
`scripts/smoke-canon-spine.mjs`, `scripts/setup-indexes.sql`,
`apps/web/app/routes/scripture.tsx` loader, `apps/web/app/routes/home.tsx`
loader + tests.

| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| CPERF-1 | Medium | `scripts/setup-indexes.sql` vs `migrate-canon-spine.mjs` L111 | `idx_verses_chapter_id` (PERF-1) was promised in the migration tx **and** `setup-indexes.sql`; only the migration tx has it — `setup-indexes.sql` still lists the old `idx_verses_chapter`. | Add `CREATE INDEX IF NOT EXISTS idx_verses_chapter_id ON lumen.verses (chapter_id, verse_number);` to `setup-indexes.sql` for parity. |
| CPERF-2 | Low | `queries.ts` `getVersesByChapter`; `smoke-canon-spine.mjs` L86 | `getVersesByChapter` now 3-way joins verses→chapters→books every click (was single-table); PK joins on two tiny tables (~87 books, ~1,582 chapters) via indexed nested loop, so cost is negligible — but smoke's "hot chapter query latency" check queries verses by `chapter_id` alone, never exercising the real 2-join shape it claims to validate. | Point the smoke latency check at the actual `VERSE_SPINE` join (or add an `EXPLAIN ANALYZE` assertion on it) so the nested-loop assumption is actually tested, not just asserted. |
| CPERF-3 | Medium | `scripture.tsx` loader L299-313; `queries.ts` `getChapterNumbers` | Real prev/next bounds (PERF-2) reuse `getChapterNumbers`, which returns every chapter row for the book — up to 138 rows for `dc` — just to `Math.max()` one scalar, on every verse click. | Add a `getChapterCount`/`MAX(number)` query returning one row, or resolve bounds via a single `chapters` row keyed on `(book_id, number)` with a computed next/prev flag. |
| CPERF-4 | Medium | `scripts/ingest-words.mjs` `VERSES_PER_BATCH = 75` | 75-verse batches over 41,995 verses ≈ 560 batches × (BEGIN+DELETE+INSERT+COMMIT) ≈ 2,000+ round trips; no wall-clock estimate appears anywhere (plan, script header, or PERF-5/MIG-7 amendment text). Math gives ~2-4 min at 50-100ms/RT — fine, but never stated. | Log/document an estimated duration (e.g. in the script header or plan) so an operator can size a deploy/maintenance window before running against prod. |
| CPERF-5 | Low | `scripts/migrate-canon-spine.mjs` L178-205 | Volumes/books insert via a per-row loop (5 + 86 = 91 round trips) despite the script using a `jsonb_to_recordset` bulk-batch pattern for chapters 20 lines later — inconsistent, though harmless at n=91. | Fine as-is at this volume; reuse the chapters' bulk-insert pattern for volumes/books if the canon ever grows enough to matter. |
| CPERF-6 | Medium | `scripture.tsx` loader `Promise.all` (verses+summary+art+chapterNumbers) | Per-verse-click query count grew from 3 (verses+summary+art) to 4 (5 with `?graph`) to add real bounds; no test asserts the count stays bounded, unlike `home.loader.test.ts`'s explicit `toHaveBeenCalledTimes` guard. | Add `toHaveBeenCalledTimes` assertions for the loader's DB calls in `scripture.loader.test.ts`, mirroring the home-loader query-count pattern. |
| CPERF-7 | Medium | `migrate-canon-spine.mjs` `SPINE_DDL` L113-114 (`idx_words_verse`, `idx_words_normalized`) | Both indexes are created on the empty `lumen.words` table during P1 (cheap to *create*), then live-maintained across all ~560 `ingest-words.mjs` batches inserting ~1.2M rows — `setup-indexes.sql`'s own header says "create tables without indexes; run this after bulk loads," which this migration doesn't follow for `words`. | Create `idx_words_normalized` (non-uniqueness-critical) after `ingest-words.mjs` completes instead of in P1's DDL; keep the `UNIQUE(verse_id, position)` index only, since correctness needs it. |
| CPERF-8 | High | `scripts/smoke-canon-spine.mjs` L58-65; `lumen.nodes` view | Edge-endpoint anti-join (DATA-5) runs `LEFT JOIN lumen.nodes` twice over ~253k edges against an un-materialized 6-way `UNION ALL` that includes the full 1.2M-row `words` table. Each branch is PK-indexed (`entities.id`, `verses.id`, `words.id`, etc.), but a `LEFT JOIN` against a plain view gives Postgres no guarantee it pushes the `id =` predicate into per-branch index scans rather than materializing/hashing the ~1.3M-row union twice. No `EXPLAIN`, timeout guard, or documented runtime — and this check gates the P4 point-of-no-return marker. | Rewrite as one `NOT EXISTS` check per source table unioned together (guaranteed indexed correlated lookups), or build a temp table of `lumen.nodes` ids once and anti-join against that; verify with `EXPLAIN ANALYZE` before relying on it to gate P4. |

| ID | Tag | Rationale (≤ 25 words) |
|---|---|---|
| CPERF-1 | material | Verified: migration L111 creates `idx_verses_chapter_id`; `setup-indexes.sql` still has stale `idx_verses_chapter`. Standalone file drifted; one-line fix. |
| CPERF-2 | noise | Both joins are PK lookups (chapters.id, books.id) — guaranteed indexed, can't meaningfully be slower. Testing the simpler shape loses nothing. |
| CPERF-3 | noise | `getChapterNumbers` returns ≤~150 rows off an indexed `book_id` column; PERF-2 already folds it into the shared round-trip. Cost is sub-millisecond. |
| CPERF-4 | noise | Docs-only nit; `ingest-words.mjs` already logs per-book `elapsedMs` live, so an upfront estimate adds no operational safety. |
| CPERF-5 | noise | Panel itself concedes "harmless at n=91"; one-time migration script, never rerun at scale. Pure style nit. |
| CPERF-6 | material | Confirmed: `scripture.loader.test.ts` has no `toHaveBeenCalledTimes`, unlike `home.loader.test.ts`'s PERF-1 guard. Same convention, cheap, protects the highest-traffic loader. |
| CPERF-7 | material | Confirmed: `idx_words_normalized` built in P1 DDL before `ingest-words.mjs`'s 1.2M-row load, contra `setup-indexes.sql`'s own "index after bulk load" header. Fix is a one-line deletion (index already covered by the documented post-load step) at real (1.2M-row) scale. |
| CPERF-8 | material | Confirmed: anti-join hash-builds the unmaterialized 6-way `lumen.nodes` view (dominated by ~1.2M words) twice, no timeout/EXPLAIN, gating P4. Worst case is safe-fail (not silent-pass), so not safety-critical, but real reliability/runtime risk on an important gate; cheap fix (statement_timeout, or per-table anti-joins). |

**Overall stance:** Half the batch is real signal, half is scale-blind nitpicking that the panel itself sometimes half-conceded (CPERF-5's "harmless at n=91") or that dissolves on inspection once you check what's actually indexed (CPERF-2's joins are PK lookups; CPERF-3's table is tiny and already folded into an existing round-trip). The four material items share a common thread worth fixing together: `setup-indexes.sql` has drifted from the real migration DDL in more than one place (CPERF-1, and implicitly CPERF-7 — plus an unflagged `idx_words_verse` vs `idx_words_verse_position` naming mismatch spotted in passing), the test suite has an inconsistent convention (CPERF-6), and the P4 gate script (CPERF-8) deserves a timeout given it hash-builds ~1.2M+ rows twice with no ceiling — none of these are safety-critical (no path to an incorrect P4 approval), but all are cheap, so fix them.

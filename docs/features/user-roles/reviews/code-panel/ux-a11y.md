Role: CODE-PANEL SPECIALIST ux-a11y (Emil design-engineering rules as house law) — feature user-roles.
Scope: all commits 17caa80..HEAD on feature/user-roles, read in full as the code now stands — admin.users.tsx + the ui primitives it consumes (input/select/badge/skeleton), root.tsx fixed chrome interaction, admin-users.server.ts param round-trips, auth-seam routes; plan.md D6-D10 UX decisions as the invariant source.
Method: full-file reads; trace of pending/fetcher/navigation state machines and focus lifecycles; hit-target arithmetic from Tailwind classes; overlap geometry computed at real widths (max-w-5xl vs fixed right-4 chrome); token check against app.css; harness run `pnpm vitest run` — admin.users.test.ts + entitlements.test.ts, 32/32 green.

| ID | Severity | Where | Problem | Fix |
|---|---|---|---|---|
| UX-A11Y-1 | high | apps/web/app/routes/admin.users.tsx:173, 318-322, 395 | Pending-state wiring is inverted. `loading = fetcher.state !== "idle"` tracks only the load-more fetcher; search/filter/sort changes go through `useSubmit` navigations (:211) which set no pending state — so during a search there is NO "Searching…" text, no `aria-busy`, no dim (the :318 `loading && rows.length === 0` branch is dead code: the fetcher only runs when rows exist). This leaves the incorporated plan spec unimplemented — plan.md:93 "searching keeps stale results rendered (`aria-busy` + opacity on the results region only)". Meanwhile load-more — the one flow that does set `loading` — marks the ENTIRE already-read results region `aria-busy` and dims it to 60%, telling SR users rows they already read are busy and flashing the list on every auto-append. And `motion-safe:opacity-60` gates a *state signal* (not motion) behind motion-safe, so reduced-motion users get no pending feedback at all. | Derive search pending from `useNavigation()` (same pathname, differing search); drive the count-bar "Searching…" and the region `aria-busy`/dim from THAT. Keep fetcher `loading` only for skeleton rows and the Load-more label. Un-gate the opacity (plain `opacity-60`; keep `motion-safe:` only on `transition-opacity`). |
| UX-A11Y-2 | high | apps/web/app/routes/admin.users.tsx:525, 520-536, 377-384 | Focus is dropped at three interaction ends, violating D6's stated "focus retained on append" (plan.md:80): (a) the Load-more sentinel sets `disabled={loading}` the instant a keyboard/SR user activates it — disabling the focused element dumps focus to `<body>` (the :182-185 comment relies on DOM identity, but the disabled attribute defeats it first); (b) on the final page `extra.cursor` goes null and the button unmounts while focused, replaced by the end-of-results `<p>` — next Tab restarts from the page top; (c) chip-remove buttons unmount on activation (:377-384) with no focus handoff. | Use `aria-disabled={loading}` + the existing idle guard (:176) instead of `disabled`; when the cursor exhausts after a keyboard activation, move focus to the "End of results" line (`tabIndex={-1}` + `.focus()`); after chip removal focus the next remaining chip or the filter group. |
| UX-A11Y-3 | med | apps/web/app/routes/admin.users.tsx:179, 189-200, 29-33 | No failed-page-fetch state. The IntersectionObserver auto-fires `fetcher.load`; a transient network error, 500, or the fail-closed 404 a DB blip produces (entitlements.server.ts:53 → requireEntitlement 404) throws to the nearest ErrorBoundary — this route deliberately exports none (D10, :29-33) — so the ROOT boundary replaces the whole page, destroying every appended page and the scroll position, with no retry (only "Back to the library"). The user did nothing but scroll. | Degrade cursor-page failures to a value: for `cursor != null` requests catch data-layer failures in the loader and return `{ rows: [], nextCursor: cursor, error: true }` (page-1/gate 404s keep throwing — D10 concealment intact), then render the sentinel as "Couldn't load more — Retry" and pause the observer until a manual click succeeds. |
| UX-A11Y-4 | med | apps/web/app/routes/admin.users.tsx:357, 546-555 | Sort *direction* is unreachable on mobile. `toggleSortTo` ignores its `_dir` param and hardcodes each column's default, and Radix Select never fires `onValueChange` when the current value is re-selected — so oldest-first joins, stalest-first activity, and Z→A email are impossible on a phone, while the md+ header buttons toggle freely (:233-239) inside the `hidden md:table` table (:437). A desktop control has no mobile equivalent. | Give the mobile select six key+dir entries (`"created:desc"` → "Joined · newest", `"created:asc"` → "Joined · oldest", …) or pair the select with a 44px direction-toggle button; either writes the same `sort`/`dir` params. |
| UX-A11Y-5 | med | apps/web/app/routes/admin.users.tsx:253, 303, 327, 343, 358, 381 | Touch targets under the 44px house law: Role/Status/Sort `SelectTrigger size="sm"` = h-7 (28px) with no hit-area overlay — and the :358 sort select is `md:hidden`, i.e. touch-first by definition; chip-remove is 16px + `after:-inset-2` = 32px (:381); clear-search is 28px + `after:-inset-1.5` = 40px (:303); sort header buttons ≈40px (20px text + `after:-inset-y-2.5`, :253). The house pattern for exactly this (44px `after:` overlay on an h-7 trigger) exists at root.tsx:80 and was not applied. | Add/expand the overlays: `relative after:absolute after:-inset-2 after:content-['']` on the three SelectTriggers (matching root.tsx), `after:-inset-3.5` on chip-remove (16+28=44), `after:-inset-2` on clear-search (28+16=44), `after:-inset-y-3` on header buttons (20+24=44). |
| UX-A11Y-6 | med | apps/web/app/routes/admin.users.tsx:263, 439-455; apps/web/app/root.tsx:173 | Sticky thead sits under the fixed chrome at real widths. thead is `sticky top-0 z-30`; the AccountChip+ThemeSelect chrome is `fixed right-4 top-4 z-40` (~130-145px wide, occupying y 16-44px; the h-9 header band occupies y 0-36px once scrolled). With `max-w-5xl` content the table's right edge runs under the chrome at every viewport from md (768px, where the table appears) up to ≈1270px — i.e. most laptops and all tablets — so the "Last seen" header and its sort button are visually covered AND click-blocked (z-40 wins). `pt-16` (:263) only protects the unscrolled state. | Keep the chrome and the sticky header out of the same band on this route: render the account/theme chrome in the page-flow header instead of fixed for /admin/users, or right-inset the table so no interactive column header can sit under the z-40 chrome below ~1280px. |
| UX-A11Y-7 | med | apps/web/app/routes/admin.users.tsx:211, 230-239, 300, 379; docs/features/user-roles/plan.md:35 | Every URL change uses `replace: true` — including discrete, deliberate actions: picking a role/status, removing a chip, toggling sort, clearing search. The plan states "URL is the state owner … (shareable, back-button correct)" (plan.md:35), but Back never undoes a filter or sort — it exits the page entirely. Replace is correct only for the debounced per-keystroke search (D7). | In `submitParams`, use `replace: !immediate`: debounced keystrokes replace; immediate (select/chip/sort/clear) changes push, so Back walks the filter history. |
| UX-A11Y-8 | low | apps/web/app/routes/admin.users.tsx:205, 276, 289 | Two search-input paper cuts: (a) `useEffect(() => setQInput(q), [q])` clobbers in-flight typing — if the "ab" navigation completes while the user has typed "abc" (debounce pending), the field snaps back to "ab" mid-typing before jumping forward again; (b) `enterKeyHint="search"` promises a search action but the form's onSubmit only preventDefaults — Enter / the iOS Search key does not flush the pending 250ms debounce. | (a) Skip the reset while the input has focus (or track a dirty flag); (b) in onSubmit, clear the timer and submit immediately. |
| UX-A11Y-9 | low | apps/web/app/routes/admin.users.tsx:398-405 | Contradictory empty state under filters: the heading keys on `q` only, so `?role=admin` (or a status filter) with zero matches renders "No users yet." while the subtext — keyed on `q \|\| activeFilters` — says "…or clear your filters." The system has users; the filter excluded them; "No users yet." is false, and its own subtext contradicts it. | Key the heading on `q \|\| activeFilters.length > 0`: "No users match these filters." vs "No users yet." |
| UX-A11Y-10 | low | apps/web/app/components/ui/skeleton.tsx:7; apps/web/app/routes/admin.users.tsx:490-514 | Reduced-motion gating on skeletons is ineffective. The Skeleton primitive bakes in un-gated `animate-pulse`; the `motion-safe:animate-pulse` added at :494 is a *second* class — tailwind-merge keeps the differently-varianted base — and the other skeleton cells (:496-511) carry no gate at all. app.css's reduced-motion block (:280) covers only view transitions, so all loading skeletons pulse under `prefers-reduced-motion: reduce`, against the house rule that every animation is reduced-motion aware. | Add `motion-reduce:animate-none` to the Skeleton primitive itself (fixes every consumer) and drop the redundant `motion-safe:animate-pulse` at :494. |

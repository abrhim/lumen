# Panel-2 adversarial review — blast-radius/rollback, personal-notes

All headline traces re-verified against the working tree before tagging.

| ID | Tag | Rationale (≤25 words + evidence) |
|---|---|---|
| BLAST-1 | material | Verified end-to-end: search.ts:679 defaults scope to `[...GROUP_KEYS]`; scope.map emits empty notes group; parseScope (search-request.server.ts:36) admits `scope=notes` → legs=[] → TypeError → caught → garbage 200. One nuance: empty-group sections return null (search.tsx:1288), but the pill row (search.tsx:1104) still surfaces "Notes" signed-out. Byte-compat break stands. |
| BLAST-2 | material | Verified: notes-harness.test.ts:8 pins `GROUP_KEYS[0]==='notes'`; notes-search-merge.test.ts:23-24 pins null-leg reference identity. Green harness would reward the F2 violation; api-search.test.ts:38 hardcoded seven keys confirmed as the tripwire to keep. |
| BLAST-3 | material | Verified: loader comment "session-free on its hot path" (scripture.tsx:598-600), in-place `delete s.media` (:603-604), both dot clusters hardcode four signals (:970-996), mobile stack budgeted "~23px" for four kinds. Separate additive loader field + degrade path + a human dot-budget ruling are plan changes, not polish — a PostgREST blip must not 500 the reader. |
| BLAST-4 | risky | The real kill switch already exists: `wrangler rollback` in seconds, safe under the additive-schema constraint (BLAST-7). Broken notes degrades a feature nobody has yet; RLS confines damage to the author. `NOTES_ENABLED` is cheap defense-in-depth worth adopting, but four gates + harness gap 5 is added surface, not a gate-blocker. |
| BLAST-5 | material | Verified: zero anon/authenticated grants today; setup-readonly-role.sql:15-16 shows `ALL TABLES` + `DEFAULT PRIVILEGES` is the house idiom — the feared careless grant is the *likely* one. `USING (true)` on collections (setup-triggers-and-rls.sql:41-43) and no-RLS user_roles/transcripts make schema exposure one grant from bypassing visibleCollections and the unshaken kill switch. Security carve-out; scoped grants + negative probe go in the plan. |
| BLAST-6 | risky | Ordering is real (config step invisible to git) but once BLAST-3's degrade paths exist, wrong order degrades to "notes quietly absent," never a crash of existing surfaces. Checklist is cheap; the crash scenario it prevents is already prevented elsewhere. |
| BLAST-7 | risky | Correct doctrine (additive-only, drop window closes at first real note; canon-spine precedent) and it underwrites BLAST-4's rollback safety. But it's a one-paragraph plan amendment with no code or design consequence beyond "don't touch existing tables" — adopt, not escalate. |
| BLAST-8 | risky | Real phenomenon (frozen origin, previewless deploys, four geometry commits on scripture.tsx in a week, live stale worktrees) and this feature maximizes exposure. Still repo process: one mandatory divergence-check line in the deploy checklist, nothing plan-structural. |
| BLAST-9 | noise | Self-labeled Low. Its actionable core (keep the three pins untouched; rewrite notes-harness) is already mandated by BLAST-2; the obs-event choice is a one-line implementation decision with no existing-user blast radius at this scale. |

## Stance

The headline trace is sound — I reproduced every link: search.ts:679 does default scope to the full GROUP_KEYS spread, parseScope does admit whatever lands in GROUP_KEYS, and the committed red harness (notes-harness.test.ts:8) does pin the exact design that makes F2 unsatisfiable, so BLAST-1/2/3/5 are correctly material and BLAST-2 must be resolved before any implementation keystroke. On the NOTES_GROUP_KEY question: the route-layer split is genuinely cleaner than a searchAll-internal guard, because the leaks live *outside* the engine — parseScope, adaptiveLimit (search.tsx:237), and the pill row (search.tsx:1104) all key off GROUP_KEYS membership, so an engine guard fixes none of them; the vocabulary fork's cost is real but bounded (signed-in /search needs explicit notes rendering since `included` and `GROUP_LABELS`/`GROUP_ICONS` are typed over GroupKey — search.tsx:330, :648) and lands in a file the plan already edits, while the signed-out renderer stays frozen by construction. Panel-1's main excess is severity inflation on the rollback trio: with additive schema, wrangler rollback is already a seconds-fast kill switch, so BLAST-4/6/7 are adopt-cheaply risks, not gates.

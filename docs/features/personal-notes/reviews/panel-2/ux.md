# UX adversarial review — personal-notes (panel-2)

Verification performed against source: SearchModal.tsx read in full;
scripture.tsx cited ranges (rail 1048-1097, sheet 1104-1146, mobile stack
971-981, register labels/rows 1283/1330/1655/1744/1836) all check out.

| ID | Tag | Rationale (≤25 words + evidence) |
|---|---|---|
| UX-1 | material | Ambiguity real and blocking (plan.md:67-69); doctrine 2/13 decide the shape. D7 leg overstated — a click-gated lazy import evades F10 — but the 75dvh mobile sheet settles it. |
| UX-2 | material | Verified: SearchModal.tsx:131-138 only navigates; no listbox, rows, or `aria-activedescendant` anywhere in the file. "Reuse" is fiction; cut-line 2 mis-costed; §5 contract owed. |
| UX-3 | material | Traced: print-nothing (§3/§4) + Desk register out (plan.md:56-57) + no palette yet = zero doors at zero notes. Fix stays typographic; doctrine 7 survives. |
| UX-4 | material | F4 covers false positives only (plan.md:130-131); recovery undesigned. Backspace `undoInputRule` is first-party PM, zero chrome; the re-fire loop is a real trap. |
| UX-5 | material | Doctrine 11 unmet as written — "selection affordance" names nothing. `[[` universal door reuses the shipped B13 pattern (SearchModal.tsx:113-129); F12 correction is concrete. |
| UX-6 | material | Gap real. Legend is licensed type per doctrine 8/12 and the shipped foot idiom (SearchModal.tsx:219-228) — not invented chrome. iOS-callout dependency honestly device-gated. |
| UX-7 | material | Stack arithmetic verified: five kinds run 30px vs four's ~23px (scripture.tsx:971-981, 4px dots, top-8). Register anatomy genuinely unspecified. First-position is preference — correctly deferred to Abram. |
| UX-8 | material | Collisions certain, not hypothetical — users will type refs inside `[[` spans. One sentence of precedence law plus F4 fixtures; cheapest prevention in the review. |
| UX-9 | material | Q6 itself promises a physical-device checklist (plan.md:183-185); this writes it. Item 8 enforces doctrine 3 forward at the cost of one plan line. |

**Stance.** This is a rare panel-1 review where every load-bearing citation
verifies against source and every proposed fix is built from existing house
idioms (register type, kbd foot line, B13 inset, CrossRefRow anatomy) rather
than new chrome — the toolbar/toast/chip temptations were explicitly refused,
so nothing earns a risky tag. Two overstatements worth the gate's eye, neither
tag-changing: UX-1's claim that D7/F10 *forbid* an in-rail editor is rhetoric
(a click-gated dynamic import would satisfy the asserted graph invariant; it
is doctrine 2 and the 75dvh keyboard-covered sheet that actually decide it),
and UX-7's first-position argument is taste dressed as inversion-logic, though
it is properly flagged as Abram's call. The nine-for-nine material count
reflects a plan that reserved surfaces without designing them, not a soft
panel-2.

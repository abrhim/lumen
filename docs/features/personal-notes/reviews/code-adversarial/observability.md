# Panel-2 adversarial — OBSERVABILITY lane

- Lane: observability (panel-1 file: `reviews/code-panel/observability.md`, IDs OBSERVABILITY-1..10)
- Date: 2026-07-30
- Tagger role: PANEL-2 ADVERSARIAL, observability lane
- Method: every cited file/line re-read on the working tree (notes.server.ts, notes.$id.tsx, scripture.tsx:325-370, api.search.tsx:150-241, search.tsx:300-399, search-obs.server.ts, NoteEditor.tsx:350-378/555-578); emission paths and field shapes verified, not taken on faith. Caller sets and constructor sites grep-verified (`getChapterNoteAnchors` has exactly one app caller; `new NoteWriteError` exists only inside `failWrite`; `"validation"` appears only in the type union).

| ID | CP | Tag | Rationale (evidence-based) |
|----|----|-----|----------------------------|
| OBSERVABILITY-1 | CP-14 | material | Verified: `failWrite` (notes.server.ts:94) logs `message: error.message.slice(0,200)` beyond the A13 pin, against the module's own header rule (:31-34). The leak vector is not hypothetical — I found a reachable path the finding didn't even name: `update` passes client-supplied `base_updated_at` into `.eq("updated_at", …)` with only a non-empty check (notes.$id.tsx:186-187, notes.server.ts:252), so garbage input yields PG 22P02 whose message echoes the client's raw string into the log; the `getChapterNoteAnchors` `.or()` filter echo (:172-174) additionally carries ref-shaped content on parse failure. `note_updated`'s prev/new timestamps are outside "ids and sizes" as claimed (:258-263). Fix loses little: pg_code preserves the error class. Concede. |
| OBSERVABILITY-2 | CP-25 | material | Verified both emissions for one failure: `getChapterNoteAnchors` routes through `failWrite` (logs `note_write_failed {op:"chapter_anchors"}` + throws, :179); the loader catch then logs `note_anchors_degraded` (scripture.tsx:359-365). Every 750ms abort (scripture.tsx:346) takes this path. Fix premise checked: scripture.tsx is the only app caller, and the loader's degraded event already captures name/message/book/chapter, so throwing raw loses no diagnostic signal — the filter-echo content a raw message could carry is book+chapter, which the event logs as fields anyway. A5 pins one event; concede. |
| OBSERVABILITY-3 | CP-15 | material | Verified end-to-end: both surfaces synthesize `{groups:[], meta:{perGroup:{}, totalMs:0, mode:"none"}}` on notesOnly (api.search.tsx:171-176, search.tsx:329-334) and pass it to `logSearchExecuted`, where zeroResult = !degraded && no after && all-groups-empty && !reference (search-obs.server.ts:59-63) — trivially true on this path, regardless of extraGroups.notes.hits. This lands in the PRE-existing OU-1 denominator with `scope: null` (looks unscoped). Real pollution of a pre-feature metric, one-line fix (`mode !== "none"` gate). Concede. |
| OBSERVABILITY-4 | CP-3 | material | Verified: in `update` with sync_anchors=1, `readAnchors` failure logs only `note_anchor_invalid_ref {ref_id}` (notes.$id.tsx:133) then falls through `if (anchors.ok)` (:217-219) to `return json({ok:true,…})` (:221) — whole diff dropped, client told success; `create` 400s on the identical condition (:166-168). The op/note_id-free event genuinely gives the operator nothing to localize save-after-save divergence. Merged into CP-3 (High/correctness) where the correctness lane independently converged; the observability half (event context + `anchors_synced:false`) stands on its own. Concede. |
| OBSERVABILITY-5 | CP-26 | material | Verified: the catch (notes.$id.tsx:353-357) contains zero logging and its comment claims data-layer classification, but `canonicalizeNoteMarkdown` (:171, :189, :256) is the realistic non-NoteWriteError thrower — prosemirror-markdown serialization is outside A3's "parse never throws" pin — plus `crypto.subtle.digest` (:193) and `deriveNoteTitle` (:281). Grep-confirmed `NoteWriteError` is constructed nowhere but `failWrite`, so any other throw is an unlogged 500 on a personal-data write path where stdout is the only signal. Three-line fix, no downside. Concede. |
| OBSERVABILITY-6 | CP-27 | material | Verified all three: (a) `classifyWriteError` (:78-85) returns only rls_denied/constraint/not_found_or_forbidden/network — `"validation"` is dead vocabulary (grep: type union only), and the `note_too_large`/`base_required` 400s emit no event, so a permanently failing oversized autosave (A13's named worst outcome) is operator-invisible. Minor overreach conceded against the finding: the `anchor_invalid` 400 DOES emit `note_anchor_invalid_ref` via readAnchors, so "no event at all" is ⅔ true — doesn't change the tag. (b) PGRST301/302 hit the `code !== ""` catch-all → "network" (:84), misfiling the token-expiry window the module's own header documents (:25-29). (c) 2200N (:80) is invalid_xml_content — unreachable for this schema. Not relitigating CF-49: it's implementation drift from the trimmed taxonomy, and the rls_denied mapping stays inside the pinned vocabulary. Concede. |
| OBSERVABILITY-7 | CP-59 | noise | The facts verify (list/get/anchors/chapter_anchors all route through `failWrite`, :120/:135/:149/:179) but the operational claim doesn't survive scrutiny: every emission carries `op`, so "list"/"get"/"anchors" are self-identifying in the log line — the read/write distinction is fully recoverable, and at this deployment (stdout logs, no alerting, single-digit DAU) nobody aggregates by event name alone. The only high-frequency read op (chapter_anchors, fires on every degraded chapter load) is removed from `note_write_failed` entirely by OBSERVABILITY-2's fix. What remains is a name-truthfulness tidy that would not change operations. |
| OBSERVABILITY-8 | CP-44 | material | Verified: the loader silently nulls an unresolvable `?anchor=` (notes.$id.tsx:72) with no event, while every POST path logs the drift event on the same condition. What tips this from tidy to material: transcript refs are `episode@t_start_s` with the A8 M3 re-window explicitly queued — a ref-grammar shift on the media capture door is a scheduled future event, not a hypothetical, and this is the exact path that would silently degrade every media capture to a blank editor. One line, already-allowlisted event. Concede. |
| OBSERVABILITY-9 | CP-60 | material | Verified both halves: canary clears only behind `d.updated_at` (NoteEditor.tsx:572-575) which a 409/400/500 body lacks — every retry re-sends `roundtrip_ok=false` and the server logs again (notes.$id.tsx:192-207), so A19's "reports once" holds only on the happy path; and the server hashes `canonical` (current buffer at save time) while len_stored/len_reserialized/first_diff describe the LOADED body — after any typing the hash is of a third body correlating with neither, voiding the forensic purpose of the gate-confirmed hash-only design. The proposed clear-after-first-carrying-submit is safe: server-side logging happens before `updateNote`, so any request reaching the action records the violation; only never-reached-the-Worker transport failures are lost, a class CF-51 already accepts as unobserved. Concede. |
| OBSERVABILITY-10 | CP-45 | material | Verified in both routes: on short-circuit the merge drops the notes group (api.search.tsx:202-205, search.tsx:349-351) but `extraGroups.notes.hits` is logged from the discarded group (:214-220, :361-367). I probed the strongest refutation — can a consumer recover shown-vs-dropped from the event itself? No: the logged `reference` is a display string; a found book-level reference also logs non-null while the group IS shown, so delivery is genuinely indistinguishable in the recorded data, and bare references ("alma 32") are a primary query shape in this app. Wrong-forever historical data in the field A4 created for purity, two-line fix. Concede — though the "funnel undercount" framing overstates present-day impact (no such funnel exists yet). |

## Carve-out downgrade suggestions

None. The lane's single high-severity finding (OBSERVABILITY-1, CP-14, obs/privacy) verified against a concretely reachable leak path (client-supplied `base_updated_at` → 22P02 echo) and is tagged material on its merits; no downgrade suggested. (Strictly, its category "privacy" is not one of the carve-out trio security/data-loss/correctness, but the question is moot — it survives on tag.)

## Overall stance

This panel-1 specialist is almost pure signal — every code-level claim I re-verified (line numbers, logged field shapes, emission paths, dead vocabulary, single-caller/single-constructor premises) was accurate, the findings self-concede their own limits (bodies-unlikely in -1, log-only scope in -10), each carries a falsification recipe, and the verified-clean section did real work. The one noise tag (-7) is an operational-impact judgment, not a factual refutation: the `op` field already carries the disambiguating information the rename would add. If anything, the lane under-claimed — the -1 leak has a cleaner reachable vector (client-supplied base timestamp echo) than the ones it cited.

| ID | Severity | Where | Problem | Fix |
|----|----------|-------|---------|-----|
| SEC-1 | high | plan.md:134-135 (H9); ingest-podcast.test.mjs:142-147 | H9 promises scrub-on-all-fatals for every stage; the only H9 test checks Deepgram request shape, not any stage's error/log path. | Add a per-stage test: throw a DSN/key-bearing error, assert the logged/console output is scrubbed before it prints. |
| SEC-2 | high | migrate-user-roles.mjs:21-27; ingest-words.mjs:28-32 (repo's only scrub() patterns) | Every existing scrub() redacts Postgres-DSN/`password=` shapes only; none has a bearer-token pattern, so a leaked DEEPGRAM_API_KEY passes through unredacted. | Add a token/API-key regex (match the raw key value or `Token \S+`) to whichever scrub() the new scripts reuse. |
| SEC-3 | high | plan.md:82-85 (Transcribe/upload) + 111-123 (stages 2-3 pipeline) | execFile/spawn inherit full `process.env` by default; if the orchestrator holds DEEPGRAM_API_KEY/DATABASE_URL while shelling out to yt-dlp, it inherits both secrets. | Pass an explicit minimal `env` (PATH only) to the yt-dlp child-process call; never let it default-inherit process.env. |
| SEC-4 | med | ingest-podcast.test.mjs:250-254 (H6 role-grant test) | H6's role-grant test checks append/idempotent-guard/no-replace via regex only; nothing asserts the UPDATE is scoped to `slug = 'admin'`. | Add an assertion that ROLE_GRANT_SQL contains a `WHERE slug = 'admin'`-equivalent predicate, not just append/guard syntax. |
| SEC-5 | med | ingest-podcast.test.mjs:127-133 (bestAudioArgs); plan.md:121-122 (H3) | No check that videoId matches `^[A-Za-z0-9_-]{11}$` before use in yt-dlp argv, and no assertion the call is argv-array (no shell). | Validate videoId against the 11-char id regex before building args; assert the invocation is execFile/spawn, not a shell string. |
| SEC-6 | med | plan.md:70-73 (stage artifacts, `<id>.deepgram.json`) | No stated limit on what the transcribe stage writes to `<id>.deepgram.json`; a full HTTP client response/error object could embed the API key. | Persist only the parsed utterances/response body to disk, never the request config, headers, or raw client error object. |
| SEC-7 | med | plan.md:92-101 (Files touched) vs 109-110 (Public contract); grant-role.mjs:54-60 | Public contract commits `admin.collections` to entitlements-keys.ts but Files touched omits it; a partial land bricks grant-role.mjs for role `admin`. | Add entitlements-keys.ts to Files touched; land the DB grant and the TS key addition in the same commit. |
| SEC-8 | low | plan.md / harness — no yt-dlp version reference anywhere; package.json has no yt-dlp entry | yt-dlp is the first non-npm external binary in this codebase (confirmed: zero prior child_process/execFile/spawn usage in scripts/), unpinned anywhere. | Record the expected yt-dlp minimum version in the show config comment; a `--version` check at discover-stage start is optional. |

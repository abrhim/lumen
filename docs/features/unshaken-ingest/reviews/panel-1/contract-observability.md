# Panel-1 review — Contract + Observability (unshaken-ingest, Phase A1)

| ID | Severity | Where | Problem (≤25 words) | Fix (≤30 words) |
|---|---|---|---|---|
| CON-1 | high | plan.md:70-73,98 vs ingest-podcast.test.mjs:113-123,182-189 | Discover's episode shape (`{id,title}`) and load's (`{videoId,subtitle,spans,uploadDate,durationS}`) never reconcile in one test — no stage documented as the transform. | Name the enrichment step (id→videoId rename, title→subtitle/spans parse) explicitly as owned by one module; add a discover→load integration fixture. |
| CON-2 | low | plan.md:98 vs ingest-podcast.test.mjs:9 | Plan's Files-touched brace list omits `parse-title.mjs`, though the harness imports `parseTitle`/`anchorsForBlock` from it directly for H1 and H7. | Add `parse-title.mjs` to the Files-touched list so its ownership is explicit rather than inferred from harness imports. |
| CON-3 | med | plan.md:67-69 vs ingest-podcast.test.mjs:9,18 | Plan calls the title grammar a per-show hook, but `parseTitle(title)` in parse-title.mjs is a hardcoded standalone function — no show-supplied grammar parameter. | Rename it as the Unshaken/CFM parser explicitly, or thread a grammar-hook argument from the show config into `parseTitle` before a second show lands. |
| CON-4 | med | media-collections.md:23-27 vs plan.md:70-73,105-107; podcast-demo.ts (proto/podcast-ui) MediaDescriptor + dateLabel | `thumbnail_url` (design's own descriptor field) is captured by no A1 stage; upload date has no home in the descriptor or the entities schema. | Capture thumbnail_url in discover (yt-dlp's thumbnail field) and pick upload date's home — metadata key or new column — before B builds the landing page. |
| CON-5 | med | ingest-podcast.test.mjs:221-230 | H8's name promises a block-label 'C' weight, but its body only asserts setweight 'A'/'B' presence and order — a load.mjs omitting C still passes. | Add `assert.ok` on a 'C' setweight plus B-before-C ordering to H8, matching the plan's stated title>subtitle>block-label rank. |
| CON-6 | low | media-collections.md:70-72 vs plan.md:78-81 | `search_index` payload `{episode}` carries no t/seq; design defers block-level deep-link routing to "the search feature" but A1's plan never restates that dependency. | Add a line to the plan's search-projections section: `?t=` block hits come from `lumen.transcripts` directly, not search_index, until the search feature decides otherwise. |
| CON-7 | high | log.server.ts:1-7 vs ingest-strongs.mjs:195,200,367,381 (+3 sibling ingest scripts) vs plan.md (no logging section) | Every existing ingest script shares a `log(event,data)` JSON-line convention with `*_done` counts and `invariant_check{pass}`; plan.md never adopts it for the podcast stages. | Adopt the house `log(event,data)` helper per stage; emit a `*_done` summary `{total,ok,skipped,degraded}` so "9/10 loaded" is answerable without reading raw stdout. |
| CON-8 | med | media-collections.md:125 vs plan.md (H1-H9, harness scope L139-142) | Design promises stages "resumable + disk-cached" but no H-numbered failure mode or harness test covers skip-if-artifact-exists logic at all. | Add H10: a stage skips only when the cached artifact exists AND passes a validity check; log the skip event with a reason. |
| CON-9 | high | media-collections.md:34-40 vs plan.md:74-77 vs ingest-podcast.test.mjs:232-239 | A1's DISCUSSES metadata puts confidence at the edge level, contradicting §rules-3's "confidence per mention, not per edge"; the harness never asserts `confidence:1` exists. | Amend §rules-3 with a title-anchor exception, or nest confidence in a synthetic mention; add the missing `confidence:1` assertion to the edges test. |

| ID | Severity | Where | Problem (≤25 words) | Fix (≤30 words) |
|---|---|---|---|---|
| COR-1 | high | plan.md L86-90 (Load); harness L202-219 (H4); schema.ts L107-127 (edges: no PK/unique) | Edges have no PK/unique constraint and no FK cascade from entities; H4 checks entities/transcripts deletes only — a missed edges delete duplicates DISCUSSES rows forever. | Add DELETE FROM lumen.edges WHERE from_id=$episodeId before insert; harness-test it; add a unique index on (from_id,to_id,rel_type,collection_id). |
| COR-2 | med | harness L90-109 (H7 anchorsForBlock); L232-239 (DISCUSSES edge test, single-id fixture + .find()) | H7 verifies anchorsForBlock returns 24 ids for Joshua, but no test confirms buildLoadPlan turns all N chapterIds into N edge rows. | Add a load-plan test asserting edge-row count (or distinct to_id set) equals chapterIds.length, using the 24-chapter Joshua fixture. |
| COR-3 | med | harness L243-248 (H6); design doc L86, L97-98 (3 CREATE INDEX statements) | H6 only checks that ONE 'CREATE INDEX IF NOT EXISTS' occurs (boolean .test()); design's schema defines three indexes needing idempotent re-run safety. | Assert MEDIA_DDL.match(/CREATE INDEX IF NOT EXISTS/g).length === 3, matching idx_transcripts_search, idx_search_tsv, and idx_search_coll. |
| COR-4 | med | plan.md L35-38 (4 grammar variants); harness L23-59 (LIVE+HOSTILE — no colon/'Book of' subtitle case) | Hostile fixtures cover ' - ' and '&' inside subtitles but not colon/'Book of' — a ranged title with that subtitle text could misroute to whole-book parsing. | Anchor whole-book detection to the prefix right after 'Come Follow Me - ', not a bare substring test; add a colon-in-subtitle hostile fixture. |
| COR-5 | med | design doc L79 (numeric(9,3)); postgres@3.4.9 README L935 ('numeric... returned as a string'); plan.md L142 (smoke ±2s check) | numeric(9,3) has ~45x margin over the longest episode (999,999.999s vs 21,960s) — fine — but postgres.js returns numeric columns as JS strings by default. | Wrap DB reads of t_start_s in Number(...) or register a custom numeric parser before any comparison; regression-test the smoke ±2s check. |
| COR-6 | low | harness L159-167 (H5 monotonic/negative tests) | H5 tests only decreasing START across utterances; an utterance starting before the PRIOR one's END (e.g. [10,20],[15,25]) passes the 'monotonic' check unnoticed. | Add an overlap assertion: utterances[i+1].start >= utterances[i].end (or document accepted Deepgram slop and assert that bound). |
| COR-7 | low | plan.md Design section (no mechanism stated); harness L61-67 (LOOKUP, test-only); ingest-openbible-refs.mjs L168-180 (live-query precedent) | Plan never states whether real bookIdByName/chapterCount comes from a live lumen.books/chapters query or a hand-copied map like the test's LOOKUP fixture. | State in plan/show-config that book and chapter lookups are queried live at load time, mirroring ingest-openbible-refs.mjs's verseCount/nextChapter pattern. |

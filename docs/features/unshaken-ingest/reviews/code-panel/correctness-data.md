# Code-panel — correctness-data (unshaken-ingest A1)

- **Role:** CODE-PANEL / SPECIALIST correctness-data.
- **Scope:** residual defects in UNEXERCISED paths after the 10/10-episode live run — re-runs over existing data, `--refresh`, `--stage` partial modes, single `--episode` mode, failure branches, boundary conditions. The mid-run batching fix (`0e485b6`) had not been reviewed.
- **Method:** full read of `plan.md` (incl. amendments + Decisions table), `bugs.md`, every `scripts/ingest-podcast/*.mjs`, `scripts/migrate-media-collections.mjs`, `scripts/smoke-media.mjs`, `scripts/__tests__/ingest-podcast.test.mjs`, and the `transcripts`/`searchIndex` defs in `packages/scripture/src/schema.ts`; traced `postgres@3.4.9`'s actual type-parser table (`node_modules/.pnpm/postgres@3.4.9/.../src/types.js`) and its `begin()`/`unsafe()` implementation rather than assuming driver behavior; empirically reproduced the whole-book block-label bug, the cross-book empty-subtitle parse, and the `--episode`-no-match crash via `node -e` against the real modules; ran one live read-only query against `lumen.books` to check a duplicate-book-name collision risk (0 collisions today — checked, not flagged below).

Verified clean, not flagged: the CHUNK=500 multi-row builder's placeholder/tuple arithmetic (each statement owns a fresh `values` array and restarts at `$1`, so chunk boundaries are correct at any `transcriptRows.length`); 0 chapterIds cannot occur post-parse (`anchorsForBlock`'s bounds check forces `start<=last`, so every span contributes ≥1 id, and `parseTitle` never returns an empty `spans` array); reversed ranges ("22-12") and chapter "0" both throw via the same bounds check (fail-closed, confirmed by trace); `bookRows`/`chapterRows`/`keytermRows` reads never assume a jsonb/numeric parse (text and explicit-`Number()`-coerced int columns only — W2's jsonb-as-string trap doesn't reach these three); `SET LOCAL` inside `sql.begin` correctly scopes to the transaction because postgres.js's `begin()` binds every callback-issued query to one reserved connection, executed in call order.

| ID | Severity | Where (file:line) | Problem (≤25 words) | Fix (≤30 words) |
|---|---|---|---|---|
| CCOR-1 | high | `scripts/ingest-podcast/index.mjs:50-81` (`discover`, no diff against DB state) + `scripts/ingest-podcast/load.mjs:16-27` (deletes scoped to the one loading episode id only) | Nothing deletes a DROPPED episode's rows; loadEpisode only touches the CURRENT episode's id, so a routine weekly `--refresh` accumulates orphaned entities/transcripts/edges forever. | After a full-window load, DELETE this collection's rows whose entity id isn't in the freshly-discovered episode list; assert the count in smoke-media before it ships. |
| CCOR-2 | high | `scripts/ingest-podcast/load.mjs:126` | Whole-book/open-ended spans (`end=null`) render as e.g. `"Joshua 1"` in the search block-label — misleadingly implies chapter 1 only, not the whole book. Hits 3/10 live episodes (Joshua, Numbers, Leviticus). | Special-case `span.end===null` to render the full/open range (e.g. `"Joshua"` or the book's last chapter) instead of falling through to the bare start chapter. |
| CCOR-3 | med | `scripts/ingest-podcast/load.mjs:35,45` (`public = EXCLUDED.public` / hardcoded `false`) | Collections upsert hardcodes `public=false` on every load; any re-run (new episode, bugfix) silently reverts a deliberate Phase-B `public=true` flip. | Exclude `public` from the per-episode UPSERT's SET clause (or COALESCE with the existing row) — only a dedicated Phase-B step should change visibility. |
| CCOR-4 | med | `scripts/ingest-podcast/index.mjs:166-170` (`loadEpisode`) | loadEpisode re-parses `ep.title` for chapterIds but stores the discover-time `ep.spans`/`subtitle` in metadata+search; a parse-title.mjs fix post-discover makes edges and metadata disagree. | Pass the freshly-computed `parsed.spans`/`parsed.subtitle` into buildLoadPlan's episode record instead of the cached `ep.spans`/`ep.subtitle` from episodes.json. |
| CCOR-5 | med | `scripts/ingest-podcast/index.mjs:120-124` (`transcribeEpisode` cache check) | transcribeEpisode's cache check validates only utterance shape/coverage; keyterm-list or model changes between runs go undetected — a stale artifact is reused silently forever. | Persist `{model, keyterms}` (or a hash) beside the `.deepgram.json` artifact and compare it during the cache-validity check to force retranscription on drift. |
| CCOR-6 | low | `scripts/ingest-podcast/parse-title.mjs:69-75` (vs. the guard at line 80) | The 2-part cross-book branch returns before the "subtitle required" guard (line 80) runs, so a title with no trailing subtitle silently parses as `subtitle=''`. | Apply the same `if (!subtitle) return null` check inside the cross-book branch before its early return. |
| CCOR-7 | low | `scripts/ingest-podcast/index.mjs:215,236,250` | `--episode=<id>` absent from the discovered window filters episodes to `[]`, so `probe=byDuration[0]` is undefined and crashes with an opaque TypeError, not a clear error. | After filtering by `--episode`, check for an empty result and throw a clear "episode not in discovered window" error before the probe step. |

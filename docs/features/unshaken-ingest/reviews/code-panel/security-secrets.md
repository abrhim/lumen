# unshaken-ingest — code-panel review (SECURITY-SECRETS specialist)

- **Role:** security/secrets of a local pipeline holding the admin DSN + Deepgram key.
- **Scope:** `scripts/ingest-podcast/*.mjs`, `scripts/migrate-media-collections.mjs`, `scripts/smoke-media.mjs`, `scripts/__tests__/ingest-podcast.test.mjs`, `apps/web/app/lib/entitlements-keys.ts`, as the code stands post-run (incl. unreviewed mid-run fixes `47bf9d2`, `0e485b6`). Lens: residual defects in paths the successful 10-episode prod run did not exercise, or that it masked.
- **Method:** traced every `scrubSecrets` call site and every `episode_failed`/`fatal` path by reading the code; empirically reproduced the `.env` regex-parse and `postgres(dsn,...)` throw behavior in Node against the real `postgres` dependency rather than assuming it; confirmed via `grep` that no `process.env.DEEPGRAM_API_KEY` assignment or lock/dotenv mechanism exists anywhere in the repo.

| ID | Severity | Where (file:line) | Problem (≤25 words) | Fix (≤30 words) |
|----|----------|--------------------|----------------------|-------------------|
| CSEC-1 | high | `scripts/ingest-podcast/index.mjs:94-113` (`fetchEpisode`, check L96 vs. download L104-107); `fetch.mjs:32-36` | `isValidAudioArtifact` check-then-download has no cross-process lock; concurrent runners (bugs.md's confirmed retry-vs-main-batch incident) can both write the same `.m4a` final path. | Claim the artifact atomically before downloading (mkdir-based lock or write-to-temp-then-`rename()` keyed by episode) so a second runner skips instead of racing. |
| CSEC-2 | medium | `scripts/ingest-podcast/index.mjs:199-210` (vs. try at L212); contrast `migrate-media-collections.mjs:97-108`, `smoke-media.mjs:17-28` | main()'s DSN/key read + `postgres(dsn,...)` sit outside the try/catch; any throw there skips scrubSecrets and crashes as a raw unhandled rejection. | Move lines 199-210 inside the try block, matching the guarded pattern already used correctly in the other two scripts. |
| CSEC-3 | medium | `scripts/ingest-podcast/util.mjs:12`; `index.mjs:208` | scrubSecrets' known-key fallback reads `process.env.DEEPGRAM_API_KEY`, but nothing ever assigns it — index.mjs:208 reads the key into a local `const` only. | Pass `apiKey` explicitly via `scrubSecrets(msg, {extraSecrets:[apiKey]})` at each call site, or have main() also set `process.env.DEEPGRAM_API_KEY` after parsing `.env`. |
| CSEC-4 | medium | `scripts/ingest-podcast/index.mjs:117-161` (`transcribeEpisode`, check L120, POST L145, write L157) | Same existsSync-then-writeFileSync race as CSEC-1: concurrent runners double-bill Deepgram, and a reader can see a truncated `deepgram.json` mid-write. | Same lock as CSEC-1, applied before the Deepgram POST; write to a temp file and `rename()` into place instead of a direct `writeFileSync`. |
| CSEC-5 | low | `index.mjs:203-204,208`; `migrate-media-collections.mjs:100`; `smoke-media.mjs:20` | The `.env` regex+`trim()` keeps surrounding quotes and inline `#` comments verbatim (verified by repro), silently corrupting a conventionally-quoted `DATABASE_URL`/`DEEPGRAM_API_KEY`. | Strip matching leading/trailing quotes and an unescaped trailing `# comment` after `trim()`; extract one shared `parseEnvFile()` helper for all three call sites. |

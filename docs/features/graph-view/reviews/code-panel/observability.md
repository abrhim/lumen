# CODE-PANEL / SPECIALIST observability — graph-view

| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| COBS-1 | High | `apps/web/app/routes/scripture.tsx:114-120` (`loadGraph` catch) | OBS-1 promised `elapsedMs` on `graph_degraded`; nothing times the call — can't tell a fast Neo4j error from a 4.5s streaming-budget timeout. | Capture `Date.now()` before `cachedJson`/`getNeighborhood`, compute `elapsedMs` in both the try and catch branches, add to log fields. |
| COBS-2 | Medium | `apps/web/app/routes/scripture.tsx:104-111`, inside the `await cachedJson(...)` success path | `graph_truncated` fires on every request that resolves truncated, including 7-day KV cache **hits** — a hub verse re-logs identical truncation on every page view, not once per computed resolution. | Have `cachedJson`/the fetcher report hit vs. miss (or log truncation inside the `getNeighborhood` fetcher lambda only), so cached reads don't re-emit. |
| COBS-3 | Medium | `apps/web/app/routes/scripture.tsx:102-103`, same success path as COBS-2 | `graph_not_found` (a cacheable `{found:false}` result) re-logs on every cache-hit replay of a mistyped/stale `?graph=` id for the full 7-day TTL, not just on first resolution. | Same fix as COBS-2: gate the log to the origin fetch, not every `cachedJson` return. |
| COBS-4 | High | `apps/web/app/components/graph/GraphOverlay.tsx:267-277`; only backstop is the route-level `ErrorBoundary` at `apps/web/app/routes/scripture.tsx:771` | `Await errorElement` only guards promise rejection, not a synchronous render crash in `GraphBody`/`ForceLayout` (e.g. a d3-force bug) — that crash bubbles to the whole-route boundary, taking down the chapter reader too, contradicting `GraphDegraded`'s own copy ("chapter behind this panel is unaffected") and emitting no `logEvent` (only React's default console). | Wrap `GraphBody`/`ForceLayout` in a local class error boundary that renders `GraphDegraded` and calls `logEvent("graph_render_crash", …)`. |
| COBS-5 | Low-Med | `scripts/backfill-neo4j-collections.mjs:218,249,262` (`node_batch_failed`, `edge_batch_failed`, `backfill_fatal`) | SEC-10 "credential scrubbing" has no explicit redaction function or test; every catch logs raw `err.message` for *any* thrown error, relying on convention (bound params, no credential-echoing query) rather than a guard. | Add a `scrub(message)` helper stripping URL-auth/basic-auth patterns before logging; assert it in `scripts/__tests__/backfill-collections.test.mjs`. |
| COBS-6 | Low | `apps/web/app/routes/scripture.tsx:102-103` vs. `:105-110` and `:114-120` | `graph_not_found` logs only `{entityId}` while sibling events on the same code path (`graph_truncated`, `graph_degraded`) also carry `depth`/`collections` — inconsistent shape makes the three related events hard to correlate in Workers Logs. | Add `depth` (and `collKey` if useful) to the `graph_not_found` fields for shape parity with its siblings. |
| COBS-7 | Low | `apps/web/app/routes/__tests__/scripture.graph.loader.test.ts` (whole file) | No harness assertion catches COBS-2/3: the loader tests never exercise a KV cache-hit path for `graph_truncated`/`graph_not_found`, so the double-log behavior shipped unnoticed. | Add a test that pre-seeds the KV mock and asserts `logEvent` is not called a second time for a truncated/not-found result already cached. |

## Notes / verified-clean

- OBS-9 (backfill per-run summary): `backfill_start` carries `startedAt, dryRun, verifyOnly`; `backfill_done` carries `startedAt, finishedAt, dryRun`, per-type and per-edge tallies — complete, matches the rejection note's "timestamp + dryRun flag suffice" (scripts/backfill-neo4j-collections.mjs:129, 254-259).
- OBS-3 (missing-`collection_id` counted once per run, silent at request time): satisfied — `nodesPerType[...].missing` and `skippedNullNodes` are logged once in `node_type_done`/`backfill_done`; nothing about collection_id gaps is logged on the request path.
- OBS-10 (`cachedJson` reused unchanged): confirmed — `loadGraph` and `loadConnections` both call the same unmodified `cachedJson` helper (apps/web/app/lib/cache.server.ts).
- Credential hygiene in `neo4jQuery`'s throw (`scripts/backfill-neo4j-collections.mjs:111-114`): only `body.errors[0].message` or `res.status` is surfaced, never the endpoint/auth header; no parameterized value in these queries is credential-bearing. No leak found, but see COBS-5 for the missing defense-in-depth.
- Batch-level progress logs (`edge_batch`, `node_type_done`) fire every 2000-row batch during a manual, sole-developer run — not flagged as over-instrumentation; this is a bounded, human-triggered script, not production request traffic.

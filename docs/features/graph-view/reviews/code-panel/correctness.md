# CODE-PANEL / SPECIALIST correctness — graph-view

Scope: `packages/scripture/src/graph/get-neighborhood.ts`, `apps/web/app/routes/scripture.tsx`,
`apps/web/app/components/graph/{GraphOverlay,ForceLayout,RadialLayout,graph-model}.tsx`.

| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| CCOR-1 | Medium | `apps/web/app/routes/scripture.tsx:318-328` | `graphNavPending` clears the instant `navHere` becomes null (critical loaderData committed), not when the deferred `graph` promise actually resolves — the "pending" signal can go stale before new data lands. | Derive pending from comparing the resolved neighborhood's id/depth to `effectiveGraphId`/`effectiveDepth`, not from navigation timing alone. |
| CCOR-2 | Medium | `apps/web/app/components/graph/ForceLayout.tsx:62-64,84-94,157-166` | Tick handler indexes `edgeRefs` by position in a `.filter()`'d `links` array, but JSX refs are keyed by index into the unfiltered `vm.edges` — any dropped edge misaligns every line drawn after it (currently masked because callers pre-filter). | Key `edgeRefs`/`links` by a stable id (`from|to|rel_type`) instead of array index. |
| CCOR-3 | Medium | `packages/scripture/src/graph/get-neighborhood.ts:144-167,228` | Depth-2/3 layer counts (`t2`,`t3`) only expand from the per-depth-capped prior frontier (`UNWIND l{d-1}`), so `truncated.total` structurally undercounts true reachable nodes for hub entities — "Showing X of Y" can understate or hide truncation. | Document `total` as an estimate in the type/comment, or compute it from the uncapped previous-layer frontier. |
| CCOR-4 | Low | `apps/web/app/components/graph/GraphOverlay.tsx:117` | `layout` useState initializer (`forceViable ? "force" : "radial"`) only runs on mount; if node count later crosses `FORCE_NODE_LIMIT` on the same instance, the Layout picker still shows "force" selected while rendering silently falls back to radial via `useForce`. | Reset `layout` in an effect keyed on `forceViable`, or drive the Segmented "checked" state from `useForce` instead of `layout`. |
| CCOR-5 | Low | `apps/web/app/components/graph/GraphOverlay.tsx:114-117` | `prefersReducedMotion` and the initial `layout` state read `window.matchMedia` synchronously during render; if this Suspense boundary is reached during SSR, client hydration can diverge from server output for reduced-motion users. | Resolve the media query via `useEffect`/`useSyncExternalStore`, defaulting to a safe SSR value. |
| CCOR-6 | Low | `apps/web/app/components/graph/graph-model.ts:22-24` | `primaryType()` picks the first label matching `TYPE_META` in Neo4j's `labels()` array order, which Neo4j does not guarantee is stable — a multi-labeled node (e.g. Verse + ChapterSummary) can flip type/color between requests. | Rank by an explicit priority list instead of raw `labels()` order. |
| CCOR-7 | Low | `apps/web/app/components/graph/GraphOverlay.tsx:150` | `isEmpty` is computed from the unfiltered `vm.nodes.length`, so hiding every neighbor type via the footer legend leaves a near-blank single-dot canvas instead of the "no connections" message. | Base `isEmpty` on `filtered.nodes.length <= 1` too. |
| CCOR-8 | Low | `apps/web/app/components/graph/graph-model.ts:110` | `hops.get(n.id) ?? 1` silently defaults any node the center-rooted BFS can't reach to hop 1, with no logging — would mask a query/data inconsistency rather than surface it. | Log/assert when a returned node has no BFS path from center instead of silently defaulting. |

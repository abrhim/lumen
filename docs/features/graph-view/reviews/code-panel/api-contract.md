# CODE-PANEL / api-contract — graph-view

| ID | Severity | Where | Problem (≤ 25 words) | Fix (≤ 30 words) |
|---|---|---|---|---|
| CAPI-1 | High | `apps/web/app/routes/scripture.tsx:196` (`GRAPH_ID_RE`) vs plan.md:46 | Charset-invalid `?graph` id resolves to `null`, so the overlay never mounts — plan promises an overlay "not found" state for invalid ids, not silent no-op. | Route invalid-but-present `graph` params to the overlay's not-found branch (e.g. keep a synthetic `{found:false}` id) instead of dropping to `null`. |
| CAPI-2 | Medium | `scripts/backfill-neo4j-collections.mjs:254-259` vs plan.md:48 | `backfill_done` report shape is `{nodes:{pgRows,matched,skippedNullCollection,perType},edges:{pgRows,matched,conflictingKeys}}` — no `missing` field for edges at all; field names don't match promised `stamped/skipped/missing`. | Add an edges `missing` count (`pgRows - matched`) and rename/alias fields to match the documented `stamped/skipped/missing` shape, or update the plan's contract line. |
| CAPI-3 | Medium | `apps/web/app/routes/scripture.tsx:70-73` vs `apps/web/app/components/graph/GraphOverlay.tsx:11-13` | `GraphPanelData` is declared twice, independently, with no import between them — only coincidentally structurally identical today. | Export one `GraphPanelData` (route or `graph-model.ts`) and import it in the other file instead of redeclaring. |
| CAPI-4 | Low | `packages/scripture/src/graph/index.ts:2-11` vs plan.md:47 | Public contract lists only `getNeighborhood`, `NeighborhoodResult`, `NeighborhoodOpts`; implementation also exports `NeighborhoodNode`, `NeighborhoodEdge`, `GRAPH_ENTITY_TYPES`, `GRAPH_NODE_TYPES`, `GRAPH_REL_TYPES`. | Either fold the extra exports into the plan's public-contract line, or keep `GRAPH_NODE_TYPES`/`GRAPH_REL_TYPES` internal since no external caller uses them. |
| CAPI-5 | Medium | `apps/web/app/routes/scripture.tsx:318-323` (`pendingGraph`) vs `:196` (`GRAPH_ID_RE`) | Client-side optimistic `?graph` parsing never runs `GRAPH_ID_RE`; a malformed id opens the overlay skeleton optimistically, then it vanishes once the loader's stricter validation resolves `graphId: null`. | Apply the same `GRAPH_ID_RE` test when computing `pendingGraph` so optimistic and server validation agree. |
| CAPI-6 | Low | `packages/scripture/src/graph/get-neighborhood.ts:8-12,134` vs plan.md:32 | `centerUnion` is built from all of `GRAPH_ENTITY_TYPES`, including `StrongsWord`/`JstReading` — plan's Scope explicitly lists "Word/Strong's node surfaces" as Out. | Exclude `StrongsWord`/`JstReading` from the center-match label union so a word id can't become a valid graph center. |
| CAPI-7 | Low | `packages/scripture/src/graph/get-neighborhood.ts:19-23` vs `packages/scripture/src/slug-map.ts:164-172` | `GRAPH_REL_TYPES` duplicates most of `RELATIONSHIP_TYPES` as an independent literal array; both were hand-edited together in this diff but nothing keeps them in sync going forward. | Derive `GRAPH_REL_TYPES` from `RELATIONSHIP_TYPES` (filtered to the semantic subset) instead of maintaining a second literal list. |
